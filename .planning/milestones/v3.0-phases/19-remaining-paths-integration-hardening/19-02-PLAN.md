---
phase: 19-remaining-paths-integration-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - tests/test_n8n_workflow_validation.py
  - tests/integration/test_package_router_integration.py
autonomous: true
requirements:
  - TST-03
  - TST-04
must_haves:
  truths:
    - "All n8n workflow JSON files are syntactically valid and structurally importable"
    - "Package Router webhook payload creates a processing_jobs row in Supabase with correct step structure"
  artifacts:
    - path: "tests/test_n8n_workflow_validation.py"
      provides: "Parametrized JSON validation for all n8n/*.json files"
      contains: "test_workflow_json"
    - path: "tests/integration/test_package_router_integration.py"
      provides: "Integration test for webhook-to-Supabase processing_jobs flow"
      contains: "test_package_router"
  key_links:
    - from: "tests/test_n8n_workflow_validation.py"
      to: "n8n/*.json"
      via: "glob + json.load + structural checks"
      pattern: "glob.*n8n.*json"
    - from: "tests/integration/test_package_router_integration.py"
      to: "scripts/payload_normalizer.py"
      via: "imports normalize_payload for testing router logic"
      pattern: "normalize_payload"
---

<objective>
Create test suite for n8n workflow JSON validation and Package Router integration testing.

Purpose: Prove all workflow files are importable (TST-03) and that the webhook-to-Supabase flow creates correct processing_jobs rows (TST-04). This is the quality gate for the entire v3.0 pipeline.
Output: test_n8n_workflow_validation.py (parametrized across all n8n/*.json), test_package_router_integration.py (mocked Supabase integration test).
</objective>

<execution_context>
@C:/Users/redle.SOULAAN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle.SOULAAN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.0-phases/19-remaining-paths-integration-hardening/19-CONTEXT.md
@.planning/milestones/v3.0-phases/19-remaining-paths-integration-hardening/19-RESEARCH.md
@.planning/milestones/v3.0-phases/19-remaining-paths-integration-hardening/19-01-SUMMARY.md

@tests/conftest.py
@tests/integration/test_ingest_flow.py
@n8n/path_e_workflow.json
@n8n/package_router_patch.json
@scripts/payload_normalizer.py

<interfaces>
<!-- From conftest.py: mock_supabase_client fixture -->
```python
@pytest.fixture
def mock_supabase_client():
    """Mock Supabase client with method chain stubs."""
    mock_client = MagicMock()
    mock_table = MagicMock()
    mock_table.select.return_value.eq.return_value.execute.return_value.data = []
    mock_table.insert.return_value.execute.return_value.data = [{"id": "test-id"}]
    mock_table.update.return_value.eq.return_value.execute.return_value.data = []
    mock_client.table.return_value = mock_table
    return mock_client
```

<!-- From Plan 01: payload_normalizer.py exports -->
```python
def parse_folder_name(folder_name: str) -> dict | None: ...
def normalize_payload(payload: dict) -> dict: ...
```

<!-- n8n workflow JSON required structure (for validation) -->
```
- Workflow files (have "nodes"): Valid JSON + nodes array + connections object + each node has id, name, type, parameters
- Config/patch files (have "template_defaults" or "_meta"): Valid JSON only, no node checks
```

<!-- Known n8n/*.json files as of Plan 01 completion -->
```
n8n/14-env-verification.json
n8n/package_router_patch.json (config file, not workflow)
n8n/path_e_workflow.json (workflow)
n8n/manual_path_workflow.json (workflow, created in 19-01)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create n8n workflow JSON validation tests (TST-03)</name>
  <files>tests/test_n8n_workflow_validation.py</files>
  <behavior>
    - Each n8n/*.json file loads without JSONDecodeError
    - Workflow files (containing "nodes" key) have nodes as a list
    - Each node in a workflow has required keys: id, name, type, parameters
    - Config/patch files (containing "_meta" or "template_defaults" but no "nodes") pass JSON syntax check only, no node validation
    - Connections object (if present) has valid structure: each value has "main" key with list of lists
    - Test is parametrized across all files in n8n/ directory using glob
    - Test handles empty n8n/ directory gracefully (no error, just no tests)
  </behavior>
  <action>
Create tests/test_n8n_workflow_validation.py following the code example from 19-RESEARCH.md.

Key implementation details:
1. Use `glob.glob(os.path.join(N8N_DIR, '*.json'))` to discover all JSON files in the n8n/ directory.
2. Parametrize with `@pytest.mark.parametrize('filepath', get_workflow_files(), ids=lambda p: os.path.basename(p))`.
3. Distinguish workflow files (have `nodes` key) from config files (have `_meta` or `template_defaults`). Config files only need JSON syntax validation.
4. For workflow files, check:
   - `nodes` is a list
   - Each node has keys: `id`, `name`, `type`, `parameters`
   - If `connections` key exists, each connection value has a `main` key
5. `REQUIRED_NODE_KEYS = {'id', 'name', 'type', 'parameters'}` — note that `position` is common but NOT required for importability.
6. N8N_DIR should be resolved relative to the test file: `os.path.join(os.path.dirname(__file__), '..', 'n8n')`.

Do NOT require a running n8n instance. This is pure file-based validation.
  </action>
  <verify>
    <automated>cd C:/Users/redle.SOULAAN/Documents/drone-pipeline && python -m pytest tests/test_n8n_workflow_validation.py -x -v</automated>
  </verify>
  <done>test_n8n_workflow_validation.py parametrically validates all n8n/*.json files. Workflow files checked for node structure. Config files checked for JSON syntax only. All current files pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Package Router integration test (TST-04)</name>
  <files>tests/integration/test_package_router_integration.py</files>
  <behavior>
    - Ingest_sorter payload normalizes and contains correct mission_id, package_type, source="ingest_sorter"
    - Folder_watcher payload normalizes with package_type parsed from folder_name, source="folder_watcher", needs_mission_lookup=True
    - For re_standard package_type, expected processing steps include color_grade and delivery_packaging
    - For construction_hybrid package_type, processing routes to manual path (no automated steps)
    - Processing job creation includes mission_id, package_type, status, and steps array
    - Steps array elements each have name and status="pending" fields
    - Unknown package_type routes to manual handling
  </behavior>
  <action>
Create tests/integration/test_package_router_integration.py following established integration test patterns from test_ingest_flow.py and the conftest.py mock_supabase_client fixture.

Key implementation details:

1. Import `normalize_payload` from `payload_normalizer` (created in Plan 01).
2. Create a helper function `build_processing_steps(package_type: str) -> list[dict]` that returns the expected steps array for a given package type. This mirrors what the Package Router would create:
   - `re_standard` / `real_estate`: `[{"name": "color_grade", "status": "pending"}, {"name": "delivery_packaging", "status": "pending"}]`
   - `mapping` / `site_survey` / `environmental_survey`: `[{"name": "mipmap_launch", "status": "pending"}, {"name": "ortho_harvest", "status": "pending"}, {"name": "delivery_packaging", "status": "pending"}]`
   - `video`: `[{"name": "v1_color", "status": "pending"}, {"name": "v1_5_metadata", "status": "pending"}, {"name": "v2_srt", "status": "pending"}, {"name": "v3_qa", "status": "pending"}, {"name": "v4_proxy", "status": "pending"}, {"name": "v5_resolve_gate", "status": "pending"}, {"name": "v6_export", "status": "pending"}, {"name": "delivery_packaging", "status": "pending"}]`
   - `construction_hybrid` / `adiat`: `[]` (manual handling, no automated steps)
   - unknown: `[]` (manual handling)

3. Create a function `simulate_router_job_creation(normalized_payload: dict, supabase_client) -> dict` that:
   - Takes normalized payload and mock Supabase client
   - Calls `build_processing_steps(normalized_payload["package_type"])`
   - Inserts a processing_jobs row via `supabase_client.table("processing_jobs").insert({...}).execute()`
   - Returns the inserted row data

4. Test functions:
   - `test_ingest_sorter_payload_creates_processing_job(mock_supabase_client)` — normalize ingest_sorter payload, simulate job creation, verify insert called with correct mission_id and steps
   - `test_folder_watcher_payload_normalizes_correctly()` — verify normalization output has needs_mission_lookup=True, package_type parsed
   - `test_processing_steps_for_each_package_type()` — parametrize over known package types, verify step arrays match expected
   - `test_manual_path_creates_empty_steps(mock_supabase_client)` — construction_hybrid and adiat produce empty steps array with status="manual"
   - `test_unknown_package_type_routes_to_manual()` — unknown package_type gets empty steps

5. Mark all tests with `@pytest.mark.integration`.
6. Use the `mock_supabase_client` fixture from conftest.py — do NOT create a new fixture.

The `build_processing_steps` and `simulate_router_job_creation` functions should live in the test file (not a separate module) since they are test utilities simulating the n8n workflow behavior.
  </action>
  <verify>
    <automated>cd C:/Users/redle.SOULAAN/Documents/drone-pipeline && python -m pytest tests/integration/test_package_router_integration.py -x -v</automated>
  </verify>
  <done>Integration test file validates: ingest_sorter payload creates correct processing_jobs row, folder_watcher payload normalizes with lookup flag, each package_type gets correct steps array, manual paths (B/D) get empty steps with manual status, unknown types route to manual.</done>
</task>

</tasks>

<verification>
- All n8n JSON validation tests pass: `pytest tests/test_n8n_workflow_validation.py -v`
- Integration tests pass: `pytest tests/integration/test_package_router_integration.py -v`
- Full test suite still green: `pytest tests/ -x -q`
- No regressions in existing tests
</verification>

<success_criteria>
- test_n8n_workflow_validation.py parametrically validates all n8n/*.json files (syntax + structure for workflows, syntax-only for config files)
- test_package_router_integration.py proves webhook payloads create correct processing_jobs rows with proper step arrays
- Both ingest_sorter and folder_watcher payload paths tested
- All known package_types have expected step mappings
- Full test suite passes without regressions
</success_criteria>

<output>
After completion, create `.planning/milestones/v3.0-phases/19-remaining-paths-integration-hardening/19-02-SUMMARY.md`
</output>
