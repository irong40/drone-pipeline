---
phase: 06-integration-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/__init__.py
  - tests/integration/test_ingest_flow.py
autonomous: true
requirements:
  - INTG-01
must_haves:
  truths:
    - "Synthetic SD card files are scanned, sorted into mission ranges, and copied into a correctly structured mission folder under tmp_path"
    - "Mission folder has all standard subfolders (photos/raw, photos/jpeg, video/full, video/proxy, video/telemetry, ppk)"
    - "Files route to correct subfolders based on extension (DNG->photos/raw, JPG->photos/jpeg, MP4->video/full, SRT->video/telemetry, MRK->ppk)"
    - "Unassigned files do not appear in any mission folder"
    - "Inventory counts reflect actual copied files"
  artifacts:
    - path: "tests/integration/__init__.py"
      provides: "Package marker for integration test directory"
    - path: "tests/integration/test_ingest_flow.py"
      provides: "End-to-end ingest integration test — 4 tests"
---

<objective>
Integration test verifying the full ingest flow: SD card scan -> sequence sorting -> mission folder creation -> file copy -> inventory count.

Purpose: Unit tests cover individual functions in isolation. This test wires them together against a real temporary filesystem to catch integration gaps.
Output: tests/integration/test_ingest_flow.py with 4 integration tests.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/redle/drone-pipeline/ingest_sorter.py
@C:/Users/redle/drone-pipeline/tests/conftest.py
@C:/Users/redle/drone-pipeline/tests/test_ingest_sorter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration test package marker</name>
  <files>tests/integration/__init__.py</files>
  <action>
    Create tests/integration/__init__.py with docstring:
    """Integration tests for the Sentinel drone post-flight pipeline."""
  </action>
  <verify>File exists and is importable.</verify>
  <done>tests/integration/__init__.py exists.</done>
</task>

<task type="auto">
  <name>Task 2: Write ingest flow integration test</name>
  <files>tests/integration/test_ingest_flow.py</files>
  <action>
    Create tests/integration/test_ingest_flow.py exercising real ingest_sorter functions
    against a synthetic SD card built in tmp_path. No filesystem mocking — all I/O is real.

    Guard import: pytest.importorskip("requests", reason="requests required for ingest_sorter")

    Import from ingest_sorter: scan_sd_card, sort_by_sequence_ranges,
    build_mission_folder_name, create_mission_structure, copy_file_to_mission, count_inventory

    FIXTURE sd_card_root(tmp_path): flat SD card with:
      DJI_0001.JPG, DJI_0002.DNG, DJI_0003.MP4, DJI_0004.SRT, DJI_0005.MRK (seq 1-5)
      DJI_0020.JPG (seq 20, outside range — unassigned)
      non_dji.txt (skipped by scanner)

    FIXTURE missions_config: one mission dict (mission_number=47, seq 1-10)

    TEST test_full_ingest_sorts_files_into_mission_subfolders:
      scan -> sort -> create_mission_structure -> copy all assigned files
      Assert 6 DJI files found, 5 assigned, 1 unassigned
      Assert all 6 standard subfolders exist
      Assert each copied file exists on disk

    TEST test_ingest_unassigned_files_not_copied:
      Only seq 20 file -> assert sorted_missions empty, unassigned has 1 item

    TEST test_ingest_multi_extension_routing:
      Verify dest paths: JPG->photos/jpeg, DNG->photos/raw, MP4->video/full,
      SRT->video/telemetry, MRK->ppk

    TEST test_inventory_count_after_ingest:
      Full flow -> count_inventory -> assert photo_count=2, video_count=1, has_ppk_data=True

    IMPORTANT: Always pass root=str(tmp_path / "incoming") to create_mission_structure.
  </action>
  <verify>python -m pytest tests/integration/test_ingest_flow.py -v — 4 passed</verify>
  <done>All 4 tests pass with real filesystem verification.</done>
</task>

</tasks>

<verification>
  python -m pytest tests/integration/test_ingest_flow.py -v
  python -m pytest tests/ -v --ignore=tests/integration  (no regressions)
</verification>

<success_criteria>
- tests/integration/__init__.py exists
- 4 tests in test_ingest_flow.py, all passing
- Real file routing verified per extension
- Unassigned files confirmed absent from mission folder
</success_criteria>

<output>
After completion, create .planning/phases/06-integration-tests/06-01-SUMMARY.md
</output>
