---
phase: 06-integration-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_delivery_flow.py
  - tests/integration/test_checkpoint_resume.py
autonomous: true
requirements:
  - INTG-03
  - INTG-04
must_haves:
  truths:
    - "delivery_packaging creates a real ZIP with correctly renamed photos and videos"
    - "ZIP internal paths follow Sentinel_{address}_{city} naming convention"
    - "gdrive_upload functions call the mocked Drive API correctly"
    - "simulated mid-pipeline failure followed by re-run skips completed files"
    - "checkpoint file accurately reflects partial completion state"
    - "after resume, all files are processed and checkpoint reflects full completion"
  artifacts:
    - path: "tests/integration/test_delivery_flow.py"
      provides: "Delivery packaging + Drive upload integration test — 4 tests"
    - path: "tests/integration/test_checkpoint_resume.py"
      provides: "Checkpoint-based resume integration test — 4 tests"
---

<objective>
Integration tests for (1) delivery packaging + Drive upload flow and (2) checkpoint-based
resume across a simulated mid-pipeline crash.

Output: test_delivery_flow.py (4 tests) and test_checkpoint_resume.py (4 tests).
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/redle/drone-pipeline/delivery_packaging.py
@C:/Users/redle/drone-pipeline/gdrive_upload.py
@C:/Users/redle/drone-pipeline/checkpoint.py
@C:/Users/redle/drone-pipeline/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write delivery flow integration test</name>
  <files>tests/integration/test_delivery_flow.py</files>
  <action>
    Real zipfile I/O against tmp_path + mock_drive_client for Drive API.

    FIXTURE complete_mission(tmp_path): mission folder with:
      photos/jpeg/ — 3 fake JPEGs
      video/exports/ — 2 fake MP4s (master_youtube.mp4, master_instagram_reels.mp4)

    TEST test_delivery_zip_contains_renamed_photos:
      collect_photos -> build manifest with rename_photo -> create_delivery_zip
      Verify with zipfile.namelist(): 3 entries, all photos/Sentinel_*_NNN.jpg

    TEST test_delivery_zip_contains_renamed_videos:
      collect_video_exports -> rename_video_export -> create_delivery_zip
      Verify PropertyTour_YouTube and PropertyTour_Reels in archive names

    TEST test_delivery_zip_full_delivery_photos_and_videos:
      Combined manifest of 3 photos + 2 videos -> create ZIP
      Verify 5 total entries, correct photos/ and video/ prefixes

    TEST test_drive_upload_calls_find_or_create_and_upload:
      Create real ZIP first, then mock Drive service.
      Call find_or_create_folder -> verify list/create calls on mock.
      Mock upload_file's resumable upload (next_chunk returns response).
  </action>
  <verify>python -m pytest tests/integration/test_delivery_flow.py -v — 4 passed</verify>
  <done>All 4 tests pass. Real ZIPs verified with zipfile.</done>
</task>

<task type="auto">
  <name>Task 2: Write checkpoint resume integration test</name>
  <files>tests/integration/test_checkpoint_resume.py</files>
  <action>
    Tests use checkpoint.py's real save/load against tmp_path — no mocking of checkpoint.

    Import: load_checkpoint, save_checkpoint, clear_checkpoint, checkpoint_path

    FIXTURE items: 5 fake file path strings
    SCRIPT_NAME = "video_color_grade"

    TEST test_checkpoint_resume_skips_completed_items:
      Run 1: process items 0-1, save checkpoint, "crash"
      Run 2: load checkpoint, process remaining items 2-4
      Assert run 2 processed exactly 3 items, no overlap with run 1
      Final checkpoint has all 5

    TEST test_checkpoint_resume_all_skipped_on_second_full_run:
      Complete all items on first run.
      Second run: nothing processed (all in checkpoint).

    TEST test_clear_checkpoint_forces_full_reprocess:
      Populate checkpoint with all 5 items.
      clear_checkpoint -> verify load returns empty set, file deleted.

    TEST test_checkpoint_atomic_write_survives_partial_state:
      Save 3 items -> read raw JSON -> verify version=1, script name, sorted list.
      Round-trip via load_checkpoint matches.
  </action>
  <verify>python -m pytest tests/integration/test_checkpoint_resume.py -v — 4 passed</verify>
  <done>All 4 tests pass. Checkpoint JSON files verified on disk.</done>
</task>

</tasks>

<verification>
  python -m pytest tests/integration/ -v  (12 passed total)
  python -m pytest tests/ -v  (all unit + integration pass)
</verification>

<success_criteria>
- test_delivery_flow.py: 4 tests, real ZIPs, correct naming
- test_checkpoint_resume.py: 4 tests, real checkpoint files, resume verified
- Full test suite passes
</success_criteria>

<output>
After completion, create .planning/phases/06-integration-tests/06-03-SUMMARY.md
</output>
