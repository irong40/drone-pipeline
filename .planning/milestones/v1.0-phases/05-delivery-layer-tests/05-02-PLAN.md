---
phase: 05-delivery-layer-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_gdrive_upload.py
  - tests/test_archive_sync.py
autonomous: true
requirements:
  - UNIT-11
  - UNIT-12
must_haves:
  truths:
    - "pytest tests/test_gdrive_upload.py passes — folder creation and file upload Drive API calls verified"
    - "pytest tests/test_archive_sync.py passes — sync, skip-existing, path-traversal guard, cleanup safety all verified"
    - "Drive API calls use mock_drive_client fixture from conftest.py"
    - "googleapiclient module is stubbed so tests run without the package installed"
  artifacts:
    - path: "tests/test_gdrive_upload.py"
      provides: "UNIT-11 test cases"
      min_lines: 80
    - path: "tests/test_archive_sync.py"
      provides: "UNIT-12 test cases"
      min_lines: 100
  key_links:
    - from: "tests/test_gdrive_upload.py"
      to: "gdrive_upload.py"
      via: "mocker.patch('gdrive_upload.get_drive_service', return_value=mock_drive_client)"
      pattern: "mock_drive_client"
    - from: "tests/test_archive_sync.py"
      to: "archive_sync.py"
      via: "mocker.patch('archive_sync.get_drive_service', return_value=mock_drive_client)"
      pattern: "mock_drive_client"
---

<objective>
Write unit tests for gdrive_upload.py (UNIT-11) and archive_sync.py (UNIT-12) using the
mock_drive_client fixture from conftest.py. Both scripts import googleapiclient at runtime
inside functions, so a sys.modules stub is required for importability in CI.

Purpose: Drive upload and archive sync have zero test coverage. The mock_drive_client
fixture in conftest.py already provides the right interface — tests only need to configure
return values and assert call shapes.
Output: tests/test_gdrive_upload.py and tests/test_archive_sync.py with ~25 total test cases.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/redle/drone-pipeline/.planning/PROJECT.md
@C:/Users/redle/drone-pipeline/.planning/ROADMAP.md
@C:/Users/redle/drone-pipeline/.planning/phases/05-delivery-layer-tests/05-01-SUMMARY.md
@C:/Users/redle/drone-pipeline/gdrive_upload.py
@C:/Users/redle/drone-pipeline/archive_sync.py
@C:/Users/redle/drone-pipeline/tests/conftest.py
@C:/Users/redle/drone-pipeline/tests/test_video_qa.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write unit tests for gdrive_upload.py</name>
  <files>tests/test_gdrive_upload.py</files>
  <action>
Replace the stub. gdrive_upload.py imports googleapiclient inside functions (lazy imports),
so stub the module in sys.modules to allow import. Use mock_drive_client from conftest.py.
Patch "gdrive_upload.get_drive_service" (not the underlying library) per conftest.py docs.

Sys.modules stub fixture (autouse, same pattern as test_video_color_grade.py):

@pytest.fixture(autouse=True)
def stub_googleapiclient(mocker):
    if "googleapiclient" not in sys.modules:
        fake_gapi = types.ModuleType("googleapiclient")
        fake_gapi.discovery = types.ModuleType("googleapiclient.discovery")
        fake_gapi.discovery.build = MagicMock()
        fake_http = types.ModuleType("googleapiclient.http")
        fake_http.MediaFileUpload = MagicMock()
        fake_http.MediaIoBaseDownload = MagicMock()
        mocker.patch.dict(sys.modules, {
            "googleapiclient": fake_gapi,
            "googleapiclient.discovery": fake_gapi.discovery,
            "googleapiclient.http": fake_http,
        })
    if "google" not in sys.modules:
        fake_google = types.ModuleType("google")
        fake_oauth2 = types.ModuleType("google.oauth2")
        fake_sa = types.ModuleType("google.oauth2.service_account")
        fake_sa.Credentials = MagicMock()
        mocker.patch.dict(sys.modules, {
            "google": fake_google,
            "google.oauth2": fake_oauth2,
            "google.oauth2.service_account": fake_sa,
        })

Test cases:

--- find_or_create_folder: folder already exists ---
test_find_or_create_folder_existing_returns_folder_id(mock_drive_client):
  Configure list().execute() with side_effect returning existing folders.
  Assert result is the deepest folder ID.

test_find_or_create_folder_creates_missing_folder(mock_drive_client):
  Configure list().execute() to return {"files": []}.
  Configure create().execute() to return {"id": "new-folder-id"}.
  Assert create was called and result is "new-folder-id".

--- upload_file ---
test_upload_file_calls_create_with_correct_metadata(mock_drive_client, tmp_path):
  Create a real tmp file. Stub MediaFileUpload.
  Mock request.next_chunk() to return (None, response_dict).
  Assert result contains correct file ID.

--- create_shareable_link ---
test_create_shareable_link_calls_permissions_create(mock_drive_client):
  Assert permissions().create called with anyone/reader body.
  Assert returned URL matches mock.

--- move_file ---
test_move_file_calls_update_with_add_remove_parents(mock_drive_client):
  Configure get().execute() to return {"parents": ["old-parent"]}.
  Assert update called with addParents/removeParents.
  </action>
  <verify>
python -m pytest tests/test_gdrive_upload.py -v --tb=short
All tests pass. Zero failures.
  </verify>
  <done>
pytest tests/test_gdrive_upload.py reports all tests PASSED. Count >= 6 test functions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for archive_sync.py</name>
  <files>tests/test_archive_sync.py</files>
  <action>
Replace the stub. archive_sync.py also has lazy googleapiclient/google.oauth2 imports.
Reuse the same autouse stub_googleapiclient pattern. Patch "archive_sync.get_drive_service".

IMPORTANT: archive_sync.py line 206 uses datetime.now(datetime.UTC) which is Python 3.11+.
In cleanup tests, patch archive_sync.datetime with a mock that provides .UTC and .now().

Test cases:

--- find_folder ---
test_find_folder_returns_id_when_found(mock_drive_client):
  Assert correct folder ID returned.

test_find_folder_returns_none_when_not_found(mock_drive_client):
  Assert None when files list is empty.

--- list_files_in_folder ---
test_list_files_in_folder_returns_all_pages(mock_drive_client):
  Simulate two pages with nextPageToken. Assert all files returned.

--- sync_delivered_to_archive ---
test_sync_downloads_new_file(mock_drive_client, tmp_path, mocker):
  Patch download_file to create dest file. Assert status == "synced".

test_sync_skips_existing_file_with_matching_size(mock_drive_client, tmp_path):
  Pre-create file with matching size. Assert status == "exists".

test_sync_dry_run_does_not_download(mock_drive_client, tmp_path, mocker):
  Assert download_file not called. Assert status == "dry_run".

test_sync_rejects_path_traversal_filename(mock_drive_client, tmp_path):
  Use "../../../etc/passwd" as filename. Assert status == "skipped_unsafe".

test_sync_empty_folder_returns_empty_list(mock_drive_client, tmp_path):
  Assert empty list returned.

--- cleanup_old_delivered ---
test_cleanup_only_deletes_verified_archived_files(mock_drive_client, mocker):
  Two old files, only one in synced_results. Assert only verified one deleted.
  Patch archive_sync.datetime for datetime.now(datetime.UTC).

test_cleanup_dry_run_does_not_delete(mock_drive_client, mocker):
  Assert delete not called but removed count is 1.

test_cleanup_skips_recent_files(mock_drive_client, mocker):
  File only 5 days old, cleanup_days=30. Assert removed == 0.
  </action>
  <verify>
python -m pytest tests/test_archive_sync.py -v --tb=short
All tests pass. Zero failures.
  </verify>
  <done>
pytest tests/test_archive_sync.py reports all tests PASSED. Count >= 11 test functions.
  </done>
</task>

</tasks>

<verification>
  python -m pytest tests/test_gdrive_upload.py tests/test_archive_sync.py -v --tb=short
  python -m pytest tests/ -v --ignore=tests/integration  (full suite, no regressions)
</verification>

<success_criteria>
- tests/test_gdrive_upload.py has >= 6 test functions covering folder creation, upload, shareable link, move
- tests/test_archive_sync.py has >= 11 test functions covering sync, skip, path-traversal, dry-run, cleanup safety
- All placeholders removed
- Full test suite (Phase 2-5) passes with 0 failures
</success_criteria>

<output>
After completion, create .planning/phases/05-delivery-layer-tests/05-02-SUMMARY.md
</output>
