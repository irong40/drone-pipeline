---
phase: 07-environment-and-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements-path-e.txt
  - test_environment.py
autonomous: true
requirements:
  - ENV-01

must_haves:
  truths:
    - "Running `python test_environment.py` in `.venv-path-e` prints 'CUDA sm_120 verified' with the RTX 5070 device name and exits 0"
    - "PyTorch 2.9.1+cu128 is installed and torch.cuda.is_available() returns True"
    - "DeepForest imports without error and its predict method is callable"
    - "rasterio, geopandas, shapely, and all geospatial deps import cleanly"
  artifacts:
    - path: "requirements-path-e.txt"
      provides: "Pinned dependency list for Path E venv"
      contains: "deepforest"
    - path: "test_environment.py"
      provides: "GPU verification script"
      contains: "CUDA sm_120 verified"
  key_links:
    - from: "test_environment.py"
      to: "torch.cuda"
      via: "assert get_device_capability()[0] >= 12"
      pattern: "get_device_capability"
    - from: "test_environment.py"
      to: "deepforest.main.deepforest"
      via: "import and instantiate"
      pattern: "deepforest"
---

<objective>
Create the dedicated Python 3.12 virtual environment for Path E with CUDA-verified PyTorch, DeepForest, and all geospatial dependencies. Build a GPU verification script that confirms the environment is production-ready.

Purpose: Path E requires PyTorch with CUDA sm_120 support (RTX 5070) and DeepForest which needs Python <3.13. A separate venv avoids conflicts with the system Python 3.14. The verification script is the gate: if it fails, no E scripts should run.
Output: requirements-path-e.txt, test_environment.py, working .venv-path-e directory.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRD-vegetation-analysis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create requirements-path-e.txt and build venv</name>
  <files>requirements-path-e.txt</files>
  <action>
Create requirements-path-e.txt with pinned dependencies for Path E. Order matters — PyTorch MUST be installed first via --index-url to get CUDA build.

The file should contain a header comment explaining the install order, then these deps:

```
# Path E Vegetation Analysis — Python 3.12 venv
# Install: pip install --index-url https://download.pytorch.org/whl/cu128 torch torchvision
# Then:    pip install -r requirements-path-e.txt

# Core ML
deepforest>=1.4.0

# Geospatial
rasterio>=1.3.0
geopandas>=0.14.0
shapely>=2.0.0
fiona>=1.9.0
pyproj>=3.6.0

# Image processing
Pillow>=10.0.0
numpy>=1.26.0

# API clients
openai>=1.0.0
requests>=2.31.0

# Reporting
reportlab>=4.0.0
matplotlib>=3.8.0
folium>=0.15.0

# Database
supabase>=2.0.0

# Utilities
python-dotenv>=1.0.0
```

Do NOT include torch/torchvision in the requirements file — they must be installed separately via --index-url to get the CUDA 12.8 build. Document this in the header comment.

Then create the venv (if it doesn't exist) and install:
```bash
python3.12 -m venv .venv-path-e
.venv-path-e/Scripts/pip install --index-url https://download.pytorch.org/whl/cu128 torch torchvision
.venv-path-e/Scripts/pip install -r requirements-path-e.txt
```

If Python 3.12 is not available on PATH as python3.12, check common Windows install paths (py -3.12, C:\Python312\python.exe). Document what worked.
  </action>
  <verify>
    <automated>.venv-path-e/Scripts/python -c "import torch; import deepforest; import rasterio; import geopandas; print('OK')"</automated>
    <manual>Verify .venv-path-e exists and has torch with CUDA support</manual>
  </verify>
  <done>Python 3.12 venv exists at .venv-path-e with PyTorch+CUDA, DeepForest, and all geospatial deps installed</done>
</task>

<task type="auto">
  <name>Task 2: Create GPU verification script</name>
  <files>test_environment.py</files>
  <action>
Create test_environment.py that verifies the full Path E environment is production-ready. The script runs inside .venv-path-e and checks:

1. Python version is 3.12.x (assert sys.version_info[:2] == (3, 12))
2. PROJ_LIB and PROJ_DATA env vars are cleared before importing rasterio (prevents QGIS conflicts on this machine)
3. PyTorch is installed with CUDA support (torch.cuda.is_available())
4. CUDA compute capability >= 12.0 for sm_120 (torch.cuda.get_device_capability()[0] >= 12)
5. GPU device name contains "5070" (sanity check)
6. DeepForest imports and can instantiate a model (deepforest.main.deepforest())
7. rasterio, geopandas, shapely, fiona, pyproj all import cleanly
8. reportlab, matplotlib, folium all import cleanly
9. openai, requests, supabase all import cleanly

On success, print JSON to stdout:
```json
{
  "status": "CUDA sm_120 verified",
  "python": "3.12.x",
  "torch": "2.9.1+cu128",
  "cuda": "12.8",
  "gpu": "NVIDIA GeForce RTX 5070",
  "deepforest": "1.x.x",
  "all_imports": true
}
```

On any failure, print the specific failure reason and exit with code 1.

Follow the pipeline convention: argparse (though no args needed), setup_logging with LOG_DIR, structured output.
  </action>
  <verify>
    <automated>.venv-path-e/Scripts/python test_environment.py</automated>
    <manual>Output should contain "CUDA sm_120 verified"</manual>
  </verify>
  <done>test_environment.py prints "CUDA sm_120 verified" with RTX 5070 device name and exits 0</done>
</task>

</tasks>

<verification>
1. `.venv-path-e/Scripts/python test_environment.py` exits 0 with "CUDA sm_120 verified"
2. `pip list` in venv shows torch, deepforest, rasterio, geopandas, reportlab, folium
3. requirements-path-e.txt exists with all dependencies listed
</verification>

<success_criteria>
- ENV-01 satisfied: Dedicated Python 3.12 venv with CUDA PyTorch on RTX 5070
- GPU verification script confirms sm_120 compute capability
- All Path E dependencies importable without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-environment-and-foundation/07-01-SUMMARY.md`
</output>
