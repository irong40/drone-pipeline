---
phase: 02-test-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - requirements.txt
  - tests/test_ingest.py
  - tests/test_ingest_sorter.py
  - tests/test_platform_detect.py
  - tests/test_folder_watcher.py
  - tests/test_folder_watcher_service.py
  - tests/test_checkpoint.py
  - tests/test_video_color_grade.py
  - tests/test_video_metadata.py
  - tests/test_srt_telemetry_parser.py
  - tests/test_video_qa.py
  - tests/test_video_proxy_gen.py
  - tests/test_video_format_export.py
  - tests/test_delivery_packaging.py
  - tests/test_gdrive_upload.py
  - tests/test_archive_sync.py
autonomous: true
requirements:
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "pytest-mock, pytest-cov, and pytest-tmp-files are declared in requirements.txt under a Dev / Testing section"
    - "Every script has a corresponding test_{script_name}.py stub file in the tests/ directory"
    - "Running `pytest tests/` completes without collection errors — all 15 stub files are importable"
    - "test_folder_watcher_service.py uses pytest.importorskip for pywin32 guard rather than bare import"
  artifacts:
    - path: "requirements.txt"
      provides: "Dev/Testing dependency block with pytest>=7.4, pytest-mock>=3.12, pytest-cov>=4.1, pytest-tmp-files"
      contains: "# Dev / Testing"
    - path: "tests/test_ingest_sorter.py"
      provides: "Stub for ingest_sorter.py — import + placeholder test"
    - path: "tests/test_platform_detect.py"
      provides: "Stub for platform_detect.py — import + placeholder test"
    - path: "tests/test_folder_watcher_service.py"
      provides: "Stub for folder_watcher_service.py — uses pytest.importorskip instead of bare import"
    - path: "tests/test_checkpoint.py"
      provides: "Stub for checkpoint.py — import + placeholder test"
    - path: "tests/test_video_color_grade.py"
      provides: "Stub for video_color_grade.py — import + placeholder test"
    - path: "tests/test_archive_sync.py"
      provides: "Stub for archive_sync.py — import + placeholder test"
  key_links:
    - from: "tests/test_folder_watcher_service.py"
      to: "pywin32"
      via: "pytest.importorskip('win32serviceutil')"
      pattern: "pytest\\.importorskip"
    - from: "requirements.txt"
      to: "pytest>=7.4"
      via: "Dev / Testing section"
      pattern: "pytest>=7\\.4"
---

<objective>
Add pytest dev dependencies to requirements.txt and create all 15 test stub files in tests/ — one per script — so `pytest tests/` collects and passes with no errors.

Purpose: Satisfies TEST-02 (dev dependencies declared) and TEST-03 (every script has a stub). After this plan, the Phase 2 success criteria are fully met and Phases 3–5 can immediately begin writing real tests.
Output: requirements.txt updated with Dev/Testing section. 15 stub files in tests/ each containing an import and a single placeholder test.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-test-infrastructure/02-RESEARCH.md
@.planning/phases/02-test-infrastructure/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dev dependencies to requirements.txt</name>
  <files>requirements.txt</files>
  <action>
Read the existing `C:/Users/redle/drone-pipeline/requirements.txt` and append a new section at the bottom:

```
# Dev / Testing
pytest>=7.4
pytest-mock>=3.12
pytest-cov>=4.1
pytest-tmp-files
```

Rationale for each:
- `pytest>=7.4`: Test runner. Requires 7.0+ for `pythonpath` config key in pytest.ini; 7.4 is stable LTS.
- `pytest-mock>=3.12`: Provides `mocker` fixture used in `mock_ffmpeg` fixture in conftest.py. Auto-teardown of patches after each test.
- `pytest-cov>=4.1`: Coverage reporting. Not used in Phase 2 but adding now so `pytest --cov` flags work immediately in Phase 3+.
- `pytest-tmp-files`: TEST-02 names this explicitly. The built-in `tmp_path` fixture will be used in actual tests (it covers all needed use cases), but this package is declared to satisfy the requirement literally with zero downside. No version pin needed.

Do NOT remove or modify any existing lines in requirements.txt. Only append the new section.
  </action>
  <verify>
Run `python -m pytest --version 2>&1` to confirm pytest is available. If not installed, note in summary that `pip install -r requirements.txt` is needed before running tests. Check that the Dev / Testing section appears at the bottom of requirements.txt with all four packages.
  </verify>
  <done>
requirements.txt contains `# Dev / Testing` section at the bottom with pytest>=7.4, pytest-mock>=3.12, pytest-cov>=4.1, and pytest-tmp-files. All existing dependencies are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 15 test stub files in tests/</name>
  <files>tests/test_ingest.py
tests/test_ingest_sorter.py
tests/test_platform_detect.py
tests/test_folder_watcher.py
tests/test_folder_watcher_service.py
tests/test_checkpoint.py
tests/test_video_color_grade.py
tests/test_video_metadata.py
tests/test_srt_telemetry_parser.py
tests/test_video_qa.py
tests/test_video_proxy_gen.py
tests/test_video_format_export.py
tests/test_delivery_packaging.py
tests/test_gdrive_upload.py
tests/test_archive_sync.py</files>
  <action>
Create 15 stub files in `C:/Users/redle/drone-pipeline/tests/`. Each file has:
1. A module docstring identifying the script, what it covers, and which phase populates it
2. An import of the script to verify importability at collection time
3. A single `test_placeholder()` function that passes

Use this standard pattern for 14 of the 15 files:

```python
"""
Unit tests for {script_name}.py — {one-line description}.
Populated in {Phase N} ({UNIT-XX}).
"""
import {module_name}  # noqa: F401 — verify importability


def test_placeholder():
    """Placeholder — replaced by real tests in {Phase N}."""
    pass
```

Create each file with these exact values:

tests/test_ingest.py:
- Script: ingest.py — MipMap photogrammetry ingest
- Phase: Phase 3 (UNIT-14)

tests/test_ingest_sorter.py:
- Script: ingest_sorter.py — SD card file sorting, sequence assignment, mission config parsing
- Phase: Phase 3 (UNIT-01)

tests/test_platform_detect.py:
- Script: platform_detect.py — EXIF drone platform detection (Mini 4 Pro, M4E, M3E)
- Phase: Phase 3 (UNIT-02)

tests/test_folder_watcher.py:
- Script: folder_watcher.py — filesystem event monitoring, debounce logic, webhook triggering
- Phase: Phase 3 (UNIT-03)

tests/test_checkpoint.py:
- Script: checkpoint.py — checkpoint manifest read/write for resume-on-failure
- Phase: Phase 3 (utility shared across all scripts)

tests/test_video_color_grade.py:
- Script: video_color_grade.py — LUT selection, FFmpeg command construction, graded_path Supabase update
- Phase: Phase 4 (UNIT-04)

tests/test_video_metadata.py:
- Script: video_metadata.py — ffprobe parsing, Supabase upsert payload, platform-specific fields
- Phase: Phase 4 (UNIT-05)

tests/test_srt_telemetry_parser.py:
- Script: srt_telemetry_parser.py — SRT frame parsing, GPS extraction, telemetry aggregation
- Phase: Phase 4 (UNIT-06)

tests/test_video_qa.py:
- Script: video_qa.py — QA threshold checks, pass/warn/fail classification, report structure
- Phase: Phase 4 (UNIT-07)

tests/test_video_proxy_gen.py:
- Script: video_proxy_gen.py — proxy resolution selection, graded fallback, FFmpeg args
- Phase: Phase 4 (UNIT-08)

tests/test_video_format_export.py:
- Script: video_format_export.py — format template loading, encoding args, Supabase status update
- Phase: Phase 4 (UNIT-09)

tests/test_delivery_packaging.py:
- Script: delivery_packaging.py — ZIP structure, address-based naming, photos-only vs video-addendum modes
- Phase: Phase 5 (UNIT-10)

tests/test_gdrive_upload.py:
- Script: gdrive_upload.py — folder creation, file upload, Drive API calls
- Phase: Phase 5 (UNIT-11)

tests/test_archive_sync.py:
- Script: archive_sync.py — age-based file filtering, sync logic, cleanup safety guards
- Phase: Phase 5 (UNIT-12)

SPECIAL CASE — tests/test_folder_watcher_service.py:
- Script: folder_watcher_service.py calls sys.exit() at module level if pywin32 is not installed
- Do NOT use bare `import folder_watcher_service` — use pytest.importorskip as a defensive guard
- Content:

```python
"""
Unit tests for folder_watcher_service.py — Windows service lifecycle (install, remove, start, stop).
Populated in Phase 3 (UNIT-13).

NOTE: folder_watcher_service.py calls sys.exit() at module level when pywin32 is not
installed. This stub uses pytest.importorskip as a defensive guard. On this Windows
machine pywin32 is available, so tests will run normally. If ever executed on a
non-Windows CI runner, this test file will be skipped cleanly instead of erroring.
"""
import pytest

# Guard: skip entire file if pywin32 is not available (non-Windows CI)
pytest.importorskip("win32serviceutil", reason="pywin32 required for Windows service tests")

import folder_watcher_service  # noqa: F401 — verify importability after pywin32 confirmed


def test_placeholder():
    """Placeholder — replaced by real tests in Phase 3 (UNIT-13)."""
    pass
```

This produces the correct behavior: on this Windows dev machine (pywin32 installed) the import succeeds and test_placeholder passes. On a hypothetical non-Windows machine, the file is skipped via SKIP (not FAILED).
  </action>
  <verify>
Run `python -m pytest tests/ --collect-only 2>&1` from `C:/Users/redle/drone-pipeline/`. Should collect exactly 15 test items (one `test_placeholder` per file) with zero errors. No ERRORS in output — only collected items or SKIPPED for folder_watcher_service if pywin32 unavailable.
  </verify>
  <done>
15 test stub files exist in tests/. Each contains a module docstring, an import of the corresponding script (or importorskip guard for folder_watcher_service), and test_placeholder(). `pytest tests/ --collect-only` shows 15 collected items and zero errors.
  </done>
</task>

</tasks>

<verification>
From `C:/Users/redle/drone-pipeline/`:
1. `cat requirements.txt` — Dev / Testing section visible at bottom with 4 packages
2. `ls tests/` — 16 files: __init__.py + conftest.py (from Plan 01) + 15 test_*.py stubs
3. `python -m pytest tests/ --collect-only` — 15 items collected, 0 errors
4. `python -m pytest tests/` — 15 passed (or 14 passed + 1 skipped if pywin32 issue)
</verification>

<success_criteria>
- requirements.txt has # Dev / Testing block with pytest>=7.4, pytest-mock>=3.12, pytest-cov>=4.1, pytest-tmp-files
- 15 test stub files exist in tests/ covering all 14 scripts + checkpoint.py
- test_folder_watcher_service.py uses pytest.importorskip guard (not bare import)
- `pytest tests/ --collect-only` shows 15 items with zero errors
- `pytest tests/` passes (all 15 test_placeholder functions pass or skip cleanly)
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-infrastructure/02-02-SUMMARY.md` with:
- Files created/modified (16 files: requirements.txt + 15 stubs)
- List of all stub files with their target phase/requirement IDs
- Note on pytest-tmp-files decision (declared to satisfy TEST-02, built-in tmp_path used in practice)
- Note on test_folder_watcher_service.py importorskip pattern
- Confirmation that `pytest tests/` passes
</output>