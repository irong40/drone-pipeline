---
phase: 18-path-v-video-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - video_color_grade.py
  - video_metadata.py
  - srt_telemetry_parser.py
  - video_qa.py
  - video_proxy_gen.py
  - video_format_export.py
autonomous: true
requirements: [PHV-01, PHV-04, PHV-05]

must_haves:
  truths:
    - "All 6 V scripts accept --processing-job-id and report step status to Supabase"
    - "Each script's step_name matches the STEP_MAP names in Package Router (v1_color, v1_5_metadata, v2_srt, v3_qa, v4_proxy, v6_export)"
    - "Scripts without --processing-job-id still work (backward compatible, reporter is no-op)"
  artifacts:
    - path: "video_color_grade.py"
      provides: "V1 color grade with PipelineStatusReporter (step_name fixed to v1_color)"
      contains: "step_name=\"v1_color\""
    - path: "video_metadata.py"
      provides: "V1.5 metadata with PipelineStatusReporter"
      contains: "step_name=\"v1_5_metadata\""
    - path: "srt_telemetry_parser.py"
      provides: "V2 SRT with PipelineStatusReporter"
      contains: "step_name=\"v2_srt\""
    - path: "video_qa.py"
      provides: "V3 QA with PipelineStatusReporter"
      contains: "step_name=\"v3_qa\""
    - path: "video_proxy_gen.py"
      provides: "V4 proxy gen with PipelineStatusReporter"
      contains: "step_name=\"v4_proxy\""
    - path: "video_format_export.py"
      provides: "V6 format export with PipelineStatusReporter"
      contains: "step_name=\"v6_export\""
  key_links:
    - from: "video_*.py / srt_telemetry_parser.py"
      to: "pipeline_status.py"
      via: "from pipeline_status import PipelineStatusReporter, add_pipeline_args"
      pattern: "PipelineStatusReporter\\(.*step_name="
---

<objective>
Add PipelineStatusReporter integration to all 6 video pipeline scripts so each step self-reports its status (running/complete/failed) to Supabase processing_jobs.

Purpose: PHV-04 requires every V-script step to update Supabase status. Currently only video_color_grade.py has PipelineStatusReporter. The other 5 scripts need it added, and video_color_grade.py needs its step_name fixed from "video_color_grade" to "v1_color" to match the STEP_MAP in the Package Router.

Output: 6 Python scripts all accepting --processing-job-id, all using correct STEP_MAP step_names, all backward compatible when --processing-job-id is omitted.
</objective>

<execution_context>
@C:/Users/redle.SOULAAN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle.SOULAAN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-package-router-core-path-a/16-02-SUMMARY.md

<interfaces>
<!-- PipelineStatusReporter integration pattern from pipeline_status.py -->

From pipeline_status.py:
```python
class PipelineStatusReporter:
    def __init__(self, processing_job_id: Optional[str], step_name: str):
        # No-op when processing_job_id is None (backward compatible)

    def start(self): ...       # Sets step status to "running"
    def complete(self): ...    # Sets step status to "complete"
    def fail(self, error_message: str): ... # Sets step status to "failed"
    def await_manual_edit(self): ...  # Sets step status to "awaiting_manual_edit"

def add_pipeline_args(parser) -> None:
    # Adds --processing-job-id optional argument to argparse parser
```

From delivery_packaging.py (established integration pattern):
```python
from pipeline_status import PipelineStatusReporter, add_pipeline_args

# In argument parser setup:
add_pipeline_args(parser)

# In main():
reporter = PipelineStatusReporter(
    processing_job_id=getattr(args, "processing_job_id", None),
    step_name="delivery_packaging",
)
if not args.dry_run:
    reporter.start()
try:
    _run_packaging(args)
    reporter.complete()
except Exception as e:
    reporter.fail(str(e))
    raise
```

STEP_MAP names (from Package Router Build Steps node):
- v1_color (video_color_grade.py)
- v1_5_metadata (video_metadata.py)
- v2_srt (srt_telemetry_parser.py)
- v3_qa (video_qa.py)
- v4_proxy (video_proxy_gen.py)
- v5_resolve_gate (manual -- no script)
- v6_export (video_format_export.py)
- delivery_packaging (delivery_packaging.py)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix video_color_grade.py step_name and add PipelineStatusReporter to video_metadata.py and srt_telemetry_parser.py</name>
  <files>video_color_grade.py, video_metadata.py, srt_telemetry_parser.py</files>
  <action>
1. In video_color_grade.py, change step_name="video_color_grade" to step_name="v1_color" (line 186). This is the ONLY change needed in this file -- PipelineStatusReporter is already fully integrated.

2. In video_metadata.py, add PipelineStatusReporter following the delivery_packaging.py pattern:
   - Add import: `from pipeline_status import PipelineStatusReporter, add_pipeline_args`
   - In the argparse setup function, call `add_pipeline_args(parser)`
   - In main(), create reporter with step_name="v1_5_metadata"
   - Wrap the core logic in try/except: reporter.start() before main work, reporter.complete() on success, reporter.fail(str(e)) on exception
   - Ensure backward compatibility: reporter is no-op when --processing-job-id is not provided

3. In srt_telemetry_parser.py, same pattern as video_metadata.py:
   - Add import for PipelineStatusReporter and add_pipeline_args
   - Call add_pipeline_args(parser) in argparse setup
   - Create reporter with step_name="v2_srt"
   - Wrap core logic with reporter.start()/complete()/fail()

For both scripts: preserve ALL existing functionality. The reporter integration is additive only. If the script uses sys.exit() for error conditions inside the main logic, convert those to raise RuntimeError() so reporter.fail() can catch them (same pattern as delivery_packaging.py in 16-02).
  </action>
  <verify>
    <automated>cd "C:\Users\redle.SOULAAN\Documents\drone-pipeline" && python -c "from video_color_grade import main; print('v1 ok')" && python -c "from video_metadata import main; print('v1.5 ok')" && python -c "from srt_telemetry_parser import main; print('v2 ok')" && python video_color_grade.py --help | findstr "processing-job-id" && python video_metadata.py --help | findstr "processing-job-id" && python srt_telemetry_parser.py --help | findstr "processing-job-id"</automated>
  </verify>
  <done>video_color_grade.py uses step_name="v1_color", video_metadata.py accepts --processing-job-id with step_name="v1_5_metadata", srt_telemetry_parser.py accepts --processing-job-id with step_name="v2_srt". All three import cleanly and show --processing-job-id in --help output.</done>
</task>

<task type="auto">
  <name>Task 2: Add PipelineStatusReporter to video_qa.py, video_proxy_gen.py, and video_format_export.py</name>
  <files>video_qa.py, video_proxy_gen.py, video_format_export.py</files>
  <action>
1. In video_qa.py, add PipelineStatusReporter:
   - Add import for PipelineStatusReporter and add_pipeline_args
   - Call add_pipeline_args(parser) in argparse setup
   - Create reporter with step_name="v3_qa"
   - Wrap core logic with reporter.start()/complete()/fail()
   - Note: video_qa.py uses --mission-id as a required arg; preserve that

2. In video_proxy_gen.py, add PipelineStatusReporter:
   - Add import for PipelineStatusReporter and add_pipeline_args
   - Call add_pipeline_args(parser) in argparse setup
   - Create reporter with step_name="v4_proxy"
   - Wrap core logic with reporter.start()/complete()/fail()

3. In video_format_export.py, add PipelineStatusReporter:
   - Add import for PipelineStatusReporter and add_pipeline_args
   - Call add_pipeline_args(parser) in argparse setup
   - Create reporter with step_name="v6_export"
   - Wrap core logic with reporter.start()/complete()/fail()

Same pattern for all three: follow delivery_packaging.py integration pattern. Convert any sys.exit() in core logic to raise RuntimeError() so reporter.fail() fires. Preserve all existing functionality and CLI args.
  </action>
  <verify>
    <automated>cd "C:\Users\redle.SOULAAN\Documents\drone-pipeline" && python -c "from video_qa import main; print('v3 ok')" && python -c "from video_proxy_gen import main; print('v4 ok')" && python -c "from video_format_export import main; print('v6 ok')" && python video_qa.py --help | findstr "processing-job-id" && python video_proxy_gen.py --help | findstr "processing-job-id" && python video_format_export.py --help | findstr "processing-job-id"</automated>
  </verify>
  <done>video_qa.py accepts --processing-job-id with step_name="v3_qa", video_proxy_gen.py accepts --processing-job-id with step_name="v4_proxy", video_format_export.py accepts --processing-job-id with step_name="v6_export". All three import cleanly and show --processing-job-id in --help output.</done>
</task>

</tasks>

<verification>
All 6 V scripts:
1. Import PipelineStatusReporter and add_pipeline_args from pipeline_status
2. Accept --processing-job-id optional argument
3. Use correct STEP_MAP step_names (v1_color, v1_5_metadata, v2_srt, v3_qa, v4_proxy, v6_export)
4. Report start/complete/fail to Supabase when --processing-job-id is provided
5. Work normally when --processing-job-id is omitted (backward compatible)
</verification>

<success_criteria>
- `python {script}.py --help` shows --processing-job-id for all 6 scripts
- `grep step_name= video_*.py srt_telemetry_parser.py` shows correct STEP_MAP names
- All scripts import cleanly without errors
- Existing tests still pass (scripts are backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/18-path-v-video-pipeline/18-01-SUMMARY.md`
</output>
