---
phase: 13-test-suite-and-acceptance
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified:
  - tests/test_vegetation_integration.py
autonomous: false
requirements:
  - TST-05
  - TST-06

must_haves:
  truths:
    - "E1→E2→E3→E4 integration test runs end-to-end with mocked APIs and produces PDF, maps, GeoJSON"
    - "delivery_packaging integration test confirms vegetation subfolder present/absent correctly"
    - "One real ortho processed through full pipeline and operator reviewed the PDF"
  artifacts:
    - path: "tests/test_vegetation_integration.py"
      provides: "Integration tests for Path E"
      contains: "test_e2e"
  key_links: []
---

<objective>
Integration tests for the full Path E pipeline and a real-ortho acceptance test.
Output: Integration test file + operator-reviewed real report.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: E1→E4 integration test + delivery packaging test</name>
  <files>tests/test_vegetation_integration.py</files>
  <action>
Integration tests with a small sample orthomosaic (create a synthetic 2048x2048 GeoTIFF test fixture) and mocked APIs.

1. **test_e2e_pipeline** — Run E1→E2→E3→E4 in sequence on sample ortho. Mock DeepForest to return 5 synthetic detections. Mock OpenAI/PlantNet to return species. Verify: Supabase rows written, PDF exists, maps exist, GeoJSON exists, JSON stdout valid for each step.

2. **test_e2e_zero_canopies** — Sample ortho with no trees (mock DeepForest returning empty). Verify: E1 exits 0, E2-E4 handle empty gracefully.

3. **test_delivery_include_vegetation** — Mock complete mission with vegetation_status='complete'. Run delivery_packaging with --include-vegetation. Verify vegetation/ subfolder in ZIP with PDF, maps, GeoJSON.

4. **test_delivery_without_vegetation** — Same mission, no --include-vegetation flag. Verify no vegetation/ in ZIP, identical to v1 output.

5. **test_delivery_incomplete_vegetation** — vegetation_status='failed'. Run with --include-vegetation. Verify no vegetation/ folder, no error.
  </action>
  <verify><automated>cd C:/Users/redle/drone-pipeline && pytest tests/test_vegetation_integration.py -v</automated></verify>
  <done>Integration tests pass for full pipeline and delivery packaging</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Path E pipeline — all 4 scripts (E1-E4) running end-to-end on a real orthomosaic</what-built>
  <how-to-verify>
1. Pick a real orthomosaic from E:\Sentinel\Output\ (a completed site_survey mission)
2. Run the full sequence:
   ```
   .venv-path-e/Scripts/python canopy_detection.py --mission-id {id} --ortho-path {path}
   .venv-path-e/Scripts/python species_classification.py --mission-id {id} --ortho-path {path} --skip-plantnet
   .venv-path-e/Scripts/python health_assessment.py --mission-id {id} --ortho-path {path}
   .venv-path-e/Scripts/python vegetation_report.py --mission-id {id} --ortho-path {path} --tier standard
   ```
3. Open the generated PDF — verify: branding, species table, health overview, maps, disclaimer
4. Check Supabase: vegetation_detections rows, vegetation_analysis_summary row
5. Open GeoJSON in QGIS — verify polygons display correctly
  </how-to-verify>
  <resume-signal>Type "approved" with notes, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. Integration tests all pass
2. Real ortho produces a complete report reviewed by operator
3. All 282 existing tests + all new tests green
</verification>

<success_criteria>TST-05, TST-06 satisfied</success_criteria>

<output>After completion, create `.planning/phases/13-test-suite-and-acceptance/13-03-SUMMARY.md`</output>
