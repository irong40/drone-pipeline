---
phase: 13-test-suite-and-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_canopy_detection.py
  - tests/test_species_classification.py
autonomous: true
requirements:
  - TST-01
  - TST-02

must_haves:
  truths:
    - "Unit tests for canopy_detection.py cover tiling, NMS, polygon export, Supabase writes with mocked GPU/rasterio"
    - "Unit tests for species_classification.py cover crop extraction, API mocking, confidence reconciliation, checkpoint resume"
    - "All tests pass with pytest and existing 282 tests remain green"
  artifacts:
    - path: "tests/test_canopy_detection.py"
      provides: "E1 unit tests"
      contains: "test_nms"
    - path: "tests/test_species_classification.py"
      provides: "E2 unit tests"
      contains: "test_reconcile"
  key_links:
    - from: "tests/test_canopy_detection.py"
      to: "canopy_detection.py"
      via: "import and mock"
      pattern: "canopy_detection"
---

<objective>
Unit tests for canopy_detection.py (E1) and species_classification.py (E2) with mocked external dependencies.

Purpose: E1 and E2 are the most complex scripts — GPU inference, tile stitching, dual API calls. Unit tests with mocks verify logic without requiring GPU or API keys.
Output: test_canopy_detection.py, test_species_classification.py passing in pytest.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@canopy_detection.py
@species_classification.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: test_canopy_detection.py</name>
  <files>tests/test_canopy_detection.py</files>
  <action>
Tests for E1. Mock rasterio dataset, deepforest model, torch.cuda, and Supabase client. Use existing conftest.py fixtures where possible.

Test cases:
1. **test_tiling_dimensions** — 2048x2048 image with tile_size=1024, overlap=128 produces correct tile count and window positions
2. **test_tiling_edge_handling** — Non-square image (3000x2000) produces tiles clamped at edges
3. **test_nms_removes_duplicates** — Two overlapping polygons with IoU > 0.3 reduces to one (higher confidence kept)
4. **test_nms_keeps_separate** — Two non-overlapping polygons both survive NMS
5. **test_pixel_to_geo_transform** — Known pixel coords + transform matrix produce expected geographic coords
6. **test_canopy_metrics** — Area, width, height calculated correctly from polygon in projected CRS
7. **test_geojson_export** — Output GeoJSON has correct schema (all required fields)
8. **test_gpkg_export** — Output GeoPackage is valid with correct CRS
9. **test_supabase_write** — Mocked client receives correct payload structure
10. **test_checkpoint_resume** — With checkpoint set, completed tiles are skipped
11. **test_zero_detections** — Empty ortho produces empty files and exit 0
12. **test_proj_env_cleanup** — PROJ_LIB/PROJ_DATA removed from os.environ at import time
  </action>
  <verify>
    <automated>cd C:/Users/redle/drone-pipeline && pytest tests/test_canopy_detection.py -v</automated>
  </verify>
  <done>All E1 unit tests pass</done>
</task>

<task type="auto">
  <name>Task 2: test_species_classification.py</name>
  <files>tests/test_species_classification.py</files>
  <action>
Tests for E2. Mock OpenAI client, PlantNet requests, rasterio, and Supabase.

Test cases:
1. **test_crop_canopy_padding** — 15% padding applied correctly, output resized to 512px max
2. **test_crop_canopy_edge** — Polygon near ortho edge doesn't raise out-of-bounds
3. **test_openai_classification** — Mocked response parsed to correct species_tag, confidence, vegetation_type
4. **test_openai_json_parse_failure** — Malformed response marked as "unidentified"
5. **test_plantnet_classification** — Mocked PlantNet response parsed correctly
6. **test_plantnet_skip** — --skip-plantnet produces no PlantNet calls
7. **test_reconcile_genus_match** — Confidence boosted +0.1, cross_validated=True
8. **test_reconcile_genus_mismatch** — Confidence reduced -0.15, cross_validated=False
9. **test_reconcile_no_plantnet** — Without PlantNet, OpenAI result returned as-is
10. **test_cost_estimation_abort** — Estimated cost > threshold aborts before API calls
11. **test_max_canopies_cap** — 300 detections capped to 200 largest by area
12. **test_checkpoint_resume** — Already-classified canopies skipped on restart
13. **test_supabase_batch_update** — Mocked client receives correct species data payload
  </action>
  <verify>
    <automated>cd C:/Users/redle/drone-pipeline && pytest tests/test_species_classification.py -v</automated>
  </verify>
  <done>All E2 unit tests pass</done>
</task>

</tasks>

<verification>
1. pytest tests/test_canopy_detection.py — all green
2. pytest tests/test_species_classification.py — all green
3. pytest tests/ — all 282 existing + new tests green
</verification>

<success_criteria>
- TST-01, TST-02 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/13-test-suite-and-acceptance/13-01-SUMMARY.md`
</output>
