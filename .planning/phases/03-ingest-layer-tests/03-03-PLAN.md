---
phase: 03-ingest-layer-tests
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_folder_watcher.py
  - tests/test_folder_watcher_service.py
autonomous: true
requirements:
  - UNIT-03
  - UNIT-13

must_haves:
  truths:
    - "pytest tests/test_folder_watcher.py passes with no failures or errors"
    - "pytest tests/test_folder_watcher_service.py passes with no failures or errors"
    - "parse_mission_number extracts mission number from SAI_MNNNN_... folder names"
    - "build_inventory counts photos, videos, and PPK files in a real tmp_path directory structure"
    - "build_inventory detected_at timestamp uses Z suffix (not +00:00)"
    - "MissionFolderHandler._reset_timer cancels previous timer and starts a new one for the same folder"
    - "MissionFolderHandler._triggered guard prevents re-triggering for already-processed folders"
    - "MissionFolderHandler.on_created fires _reset_timer for new files in mission subfolders"
    - "fire_webhook (folder_watcher) posts inventory dict and returns True on success, False on RequestException"
    - "SentinelFolderWatcherService.SvcStop sets running=False and calls win32event.SetEvent"
    - "SentinelFolderWatcherService.SvcDoRun sets running=True and calls self.main()"
  artifacts:
    - path: "tests/test_folder_watcher.py"
      provides: "UNIT-03 test coverage for folder_watcher.py"
      min_lines: 100
    - path: "tests/test_folder_watcher_service.py"
      provides: "UNIT-13 test coverage for folder_watcher_service.py"
      min_lines: 60
  key_links:
    - from: "tests/test_folder_watcher.py"
      to: "threading.Timer"
      via: "mocker.patch to prevent 60s real sleep"
      pattern: "mocker.patch.*threading.Timer"
    - from: "tests/test_folder_watcher.py"
      to: "requests.post"
      via: "mocker.patch in fire_webhook tests"
      pattern: "mocker.patch.*requests.post"
    - from: "tests/test_folder_watcher_service.py"
      to: "win32serviceutil.ServiceFramework.__init__"
      via: "__new__ instantiation or mocker.patch to bypass pywin32 OS calls"
      pattern: "__new__|mocker.patch.*ServiceFramework"
---

<objective>
Replace the stub test files for folder_watcher.py and folder_watcher_service.py with real unit tests covering debounce logic, event filtering, webhook payload correctness, and Windows service lifecycle.

Purpose: Verify UNIT-03 (debounce timer control, event filtering, webhook payload with Z-suffix timestamp) and UNIT-13 (service install/remove, SvcStop/SvcDoRun lifecycle) without real threading sleeps, real OS events, or Win32 API calls.

Output: Populated tests/test_folder_watcher.py and tests/test_folder_watcher_service.py with passing test suites.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ingest-layer-tests/03-RESEARCH.md
</context>

<feature>
  <name>folder_watcher.py unit tests (UNIT-03)</name>
  <files>tests/test_folder_watcher.py, folder_watcher.py</files>
  <behavior>
Replace the test_placeholder() stub with real tests. Keep both importorskip guards
(requests + watchdog) and `import folder_watcher` at the top.

Import only specific functions and classes under test:
  from folder_watcher import (
      parse_mission_number, build_inventory, fire_webhook, MissionFolderHandler,
  )

CRITICAL: Never let a real threading.Timer run. DEBOUNCE_SECONDS = 60.
Always patch: mocker.patch("folder_watcher.threading.Timer")

PITFALL: MissionFolderHandler._triggered set prevents re-triggering.
Create a fresh MissionFolderHandler instance in each test function.

Tests to write:

--- parse_mission_number ---
Cases:
  "SAI_M0047_re_standard_20260218" -> 47
  "SAI_M0001_video_20260101"       -> 1
  "SAI_M9999_mapping_20260301"     -> 9999
  "NOT_A_MISSION_FOLDER"           -> None  (no match)
  "SAI_M_broken"                   -> None  (no digits after M)

--- build_inventory (tmp_path) ---
Cases:
  (a) Mixed files: Create tmp_path/photos/jpeg/DJI_0001.JPG,
      tmp_path/video/full/DJI_0001.MP4, tmp_path/ppk/DJI_0001.MRK.
      Name tmp_path folder as "SAI_M0047_re_standard_20260218" by using
      tmp_path / "SAI_M0047_re_standard_20260218" as the mission folder root.
      Assert photo_count=1, video_count=1, has_ppk_data=True, mission_number=47.
      Assert "detected_at" ends with "Z" (not "+00:00").
  (b) Empty folder: No files. Assert photo_count=0, video_count=0, has_ppk_data=False.
  (c) Photos only: Only DNG files. Assert photo_count>0, has_ppk_data=False.
  (d) total_size_bytes: With one real file, assert total_size_bytes > 0.

--- fire_webhook ---
Cases:
  (a) Success: mocker.patch("requests.post") returns mock with status_code=200,
      raise_for_status() does nothing.
      inventory = {"folder_name": "SAI_M0047...", "photo_count": 3, ...}
      Assert fire_webhook(inventory) returns True.
      Assert requests.post called once with json=inventory.
  (b) RequestException: mock raise_for_status raises requests.RequestException.
      Assert fire_webhook(inventory) returns False.

--- MissionFolderHandler._reset_timer ---
Cases:
  (a) Cancels previous timer and starts new one:
      mock_timer_class = mocker.patch("folder_watcher.threading.Timer")
      mock_timer1, mock_timer2 = MagicMock(), MagicMock()
      mock_timer_class.side_effect = [mock_timer1, mock_timer2]
      handler = MissionFolderHandler(watch_dir="/tmp/watch", debounce_seconds=60)
      Call handler._reset_timer("SAI_M0001_re_standard_20260218") twice.
      Assert mock_timer1.cancel.assert_called_once()  (first timer cancelled on second call)
      Assert mock_timer2.start.assert_called_once()   (second timer started)
  (b) _triggered guard: Call _reset_timer once (timer starts). Then add folder to
      handler._triggered manually. Call _reset_timer again for same folder.
      Assert second call does NOT create another timer (mock_timer_class.call_count == 1).
  (c) Timer daemon flag: Verify timer1.daemon is set to True after _reset_timer call.
      mock_timer1.daemon is settable (MagicMock supports attribute assignment).
      Use handler._timers to get the stored timer and check .daemon attribute was set.
      Simpler: after first _reset_timer, verify mock_timer1.start.called is True
      and mock_timer1.daemon was assigned True (mock records attribute sets).

--- MissionFolderHandler.on_created ---
Cases:
  (a) New top-level mission folder event: Create a mock event with is_directory=True,
      src_path="/watch/SAI_M0001_re_standard_20260218".
      Patch handler.watch_dir = "/watch".
      Patch _reset_timer on the handler instance (mocker.patch.object).
      Call handler.on_created(event).
      Assert _reset_timer called with "SAI_M0001_re_standard_20260218".
  (b) File event inside mission folder: Create mock event with is_directory=False,
      src_path="/watch/SAI_M0001_re_standard_20260218/photos/jpeg/DJI_0001.JPG".
      Assert _reset_timer called with "SAI_M0001_re_standard_20260218".
  (c) Directory event that is NOT top-level (nested subdir):
      is_directory=True, src_path="/watch/SAI_M0001_re_standard_20260218/photos".
      src_path directory is not watch_dir itself. Expect _reset_timer NOT called for
      new top-level dir path, but the file's parent folder will be None from _get_mission_folder.
      Actually: on_created returns early for non-top-level dirs (check source).
      Expect _reset_timer called 0 times.

--- MissionFolderHandler._on_debounce_complete ---
  Patch build_inventory to return a fake inventory dict.
  Patch fire_webhook.
  handler = MissionFolderHandler(watch_dir=str(tmp_path), debounce_seconds=60)
  Call handler._on_debounce_complete("SAI_M0001_re_standard_20260218") directly.
  Assert folder is added to handler._triggered.
  Assert fire_webhook called once.
  Assert build_inventory called with the expected folder path.
  </behavior>
  <implementation>
    Replace stub contents. Keep both importorskip guards + import folder_watcher.
    All MissionFolderHandler tests: fresh instance per test (no shared state).
    Always patch "folder_watcher.threading.Timer" before instantiating handler — otherwise
    the watchdog/FileSystemEventHandler superclass may be an issue; actually MissionFolderHandler
    inherits FileSystemEventHandler which has no __init__ side effects that require patching.
    For on_created tests: create a simple mock event using MagicMock() with is_directory and
    src_path attributes set.
    Use mocker.patch.object(handler, "_reset_timer") to mock the method on a specific instance.
  </implementation>
</feature>

<feature>
  <name>folder_watcher_service.py unit tests (UNIT-13)</name>
  <files>tests/test_folder_watcher_service.py, folder_watcher_service.py</files>
  <behavior>
Replace the test_placeholder() stub with real tests. Keep the importorskip("win32serviceutil")
guard and `import folder_watcher_service` at the top.

Import the class under test:
  from folder_watcher_service import SentinelFolderWatcherService

CRITICAL: SentinelFolderWatcherService.__init__ calls win32serviceutil.ServiceFramework.__init__
and win32event.CreateEvent. In tests, bypass __init__ entirely using __new__:
  svc = SentinelFolderWatcherService.__new__(SentinelFolderWatcherService)
Then set attributes manually that __init__ would have set.

Alternative: mock win32serviceutil.ServiceFramework.__init__ to return None, and
mock win32event.CreateEvent to return MagicMock(). Either approach is acceptable.

Tests to write:

--- Service class attributes ---
  Assert SentinelFolderWatcherService._svc_name_ == "SentinelFolderWatcher"
  Assert SentinelFolderWatcherService._svc_display_name_ contains "Sentinel"
  Assert SentinelFolderWatcherService._svc_description_ is a non-empty string

--- SvcStop ---
  Use __new__ pattern:
    svc = SentinelFolderWatcherService.__new__(SentinelFolderWatcherService)
    svc.stop_event = MagicMock()
    svc.running = True
    Patch win32event.SetEvent (mocker.patch("win32event.SetEvent"))
    Patch svc.ReportServiceStatus (mocker.patch.object(svc, "ReportServiceStatus"))
    Call svc.SvcStop()
    Assert svc.running is False
    Assert win32event.SetEvent called once with svc.stop_event

--- SvcDoRun ---
  Use __new__ pattern. Patch svc.main (mocker.patch.object(svc, "main")).
  Patch servicemanager.LogMsg (mocker.patch("servicemanager.LogMsg")).
  svc.running = False  (will be set True by SvcDoRun)
  Call svc.SvcDoRun()
  Assert svc.running is True
  Assert svc.main.called (SvcDoRun calls self.main())
  Assert servicemanager.LogMsg called once

--- Service constants ---
  Assert SentinelFolderWatcherService._svc_name_ == "SentinelFolderWatcher"
  (Simple attribute read — no mock needed)
  </behavior>
  <implementation>
    Replace stub contents. Keep importorskip("win32serviceutil") guard + import folder_watcher_service.
    Use __new__ pattern for all instantiation tests (bypass __init__ OS calls).
    Set required instance attributes (stop_event, running) manually after __new__.
    Patch win32event and servicemanager at their module paths: "win32event.SetEvent",
    "servicemanager.LogMsg" — not at the import-site in folder_watcher_service.
    Keep tests focused on lifecycle logic: running flag, method calls, attribute values.
    Do NOT test main() internals (which starts a real Observer/watchdog thread).
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: RED — Write failing tests for folder_watcher.py (UNIT-03) and folder_watcher_service.py (UNIT-13)</name>
  <files>tests/test_folder_watcher.py, tests/test_folder_watcher_service.py</files>
  <action>
Write the complete test bodies for both files, replacing test_placeholder() in each.

For tests/test_folder_watcher.py:
  Keep: pytest.importorskip("requests") + pytest.importorskip("watchdog") + import folder_watcher.
  Add imports: import os; from unittest.mock import MagicMock; import requests
  Add function imports: from folder_watcher import parse_mission_number, build_inventory, fire_webhook, MissionFolderHandler
  Write all tests described in the feature behavior sections above.
  ALWAYS patch "folder_watcher.threading.Timer" in any test that instantiates MissionFolderHandler
  AND calls _reset_timer — or any test that might trigger timer creation.
  For build_inventory tests: create the mission folder inside tmp_path, e.g.:
    mission_path = tmp_path / "SAI_M0047_re_standard_20260218"
    (mission_path / "photos" / "jpeg").mkdir(parents=True)
    (mission_path / "photos" / "jpeg" / "DJI_0001.JPG").write_bytes(b"fake")
    result = build_inventory(str(mission_path))

For tests/test_folder_watcher_service.py:
  Keep: pytest.importorskip("win32serviceutil") + import folder_watcher_service.
  Add imports: from unittest.mock import MagicMock; import win32event; import servicemanager
  Add class import: from folder_watcher_service import SentinelFolderWatcherService
  Write service attribute, SvcStop, and SvcDoRun tests using the __new__ pattern.

After writing, run: pytest tests/test_folder_watcher.py tests/test_folder_watcher_service.py -v
These should PASS (source functions exist). If any test fails due to incorrect assumptions
about return values or attribute names, read the source and fix the assertion.
  </action>
  <verify>pytest tests/test_folder_watcher.py tests/test_folder_watcher_service.py -v 2>&amp;1 | tail -30</verify>
  <done>All tests in both files pass. pytest exit code 0. No test_placeholder() functions remain. test_folder_watcher.py has at least 8 distinct test functions. test_folder_watcher_service.py has at least 4 distinct test functions.</done>
</task>

</tasks>

<verification>
Run: pytest tests/test_folder_watcher.py tests/test_folder_watcher_service.py -v
Expected: All tests pass, exit code 0, no test_placeholder() remaining.

Run: pytest tests/test_folder_watcher.py tests/test_folder_watcher_service.py --co -q
Confirms collection without import errors.

Run: pytest tests/ -v
Confirms no regressions across the full test suite (all 5 ingest test files + Phase 2 stubs for other scripts).
</verification>

<success_criteria>
- pytest tests/test_folder_watcher.py exits 0 with at least 8 passing tests
- pytest tests/test_folder_watcher_service.py exits 0 with at least 4 passing tests
- parse_mission_number: valid + None cases tested
- build_inventory: mixed files, empty folder, detected_at Z-suffix verified (tmp_path)
- fire_webhook: success + RequestException failure cases
- _reset_timer: timer cancellation + re-trigger guard + timer.daemon assignment
- on_created: top-level dir event, file event, nested-dir event (ignored) cases
- _on_debounce_complete: _triggered set, build_inventory called, fire_webhook called
- SvcStop: running=False set, win32event.SetEvent called
- SvcDoRun: running=True set, self.main() called, servicemanager.LogMsg called
- threading.Timer never actually sleeps in any test (always patched)
</success_criteria>

<output>
After completion, create .planning/phases/03-ingest-layer-tests/03-03-SUMMARY.md
</output>
