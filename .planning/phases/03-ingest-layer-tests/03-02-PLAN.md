---
phase: 03-ingest-layer-tests
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_platform_detect.py
autonomous: true
requirements:
  - UNIT-02

must_haves:
  truths:
    - "pytest tests/test_platform_detect.py passes with no failures or errors"
    - "detect_from_exiftool correctly identifies Mini 4 Pro (FC8282), M4E (FC9100), and M3E (FC8482) from mocked ExifToolHelper"
    - "detect_from_exiftool returns (None, meta) for unrecognized model tags"
    - "detect_from_exif falls back to PIL.Image.getexif when exiftool returns (None, None)"
    - "detect_from_ffprobe parses ffprobe JSON stdout and returns correct platform from FFPROBE_MODEL_PATTERNS"
    - "detect_from_ffprobe returns None when returncode != 0"
    - "_extract_metadata_text flattens format tags and stream tags into a single searchable string"
    - "detect_platform_from_folder uses EXIF path when photos/jpeg subfolder contains files"
  artifacts:
    - path: "tests/test_platform_detect.py"
      provides: "UNIT-02 test coverage for platform_detect.py"
      min_lines: 100
  key_links:
    - from: "tests/test_platform_detect.py"
      to: "exiftool.ExifToolHelper"
      via: "mocker.patch context manager protocol"
      pattern: "mocker.patch.*ExifToolHelper"
    - from: "tests/test_platform_detect.py"
      to: "subprocess.run"
      via: "mock_ffmpeg fixture or mocker.patch"
      pattern: "subprocess.run|mock_ffmpeg"
    - from: "tests/test_platform_detect.py"
      to: "PIL.Image.open"
      via: "mocker.patch for Pillow fallback path"
      pattern: "mocker.patch.*PIL.Image.open"
---

<objective>
Replace the stub test file for platform_detect.py with real unit tests covering EXIF detection (pyexiftool), Pillow fallback, ffprobe video detection, and the folder-level consensus logic.

Purpose: Verify UNIT-02 — that Mini 4 Pro, M4E, and M3E are correctly identified from mocked EXIF and ffprobe metadata, and that the fallback chain works correctly.

Output: Populated tests/test_platform_detect.py with a passing test suite.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ingest-layer-tests/03-RESEARCH.md
</context>

<feature>
  <name>platform_detect.py unit tests (UNIT-02)</name>
  <files>tests/test_platform_detect.py, platform_detect.py</files>
  <behavior>
Replace the test_placeholder() stub with real tests. No importorskip guard needed in the
stub — platform_detect.py has no module-level sys.exit(). Keep `import platform_detect`.

Import only specific functions under test:
  from platform_detect import (
      detect_from_exiftool, detect_from_exif, detect_from_ffprobe,
      _extract_metadata_text, detect_platform_from_folder,
  )

CRITICAL MOCK PATTERN for pyexiftool (context manager protocol):
  ExifToolHelper is used as: `with exiftool.ExifToolHelper() as et:`
  Correct mock setup:
    mock_et_class = mocker.patch("exiftool.ExifToolHelper")
    mock_et_instance = MagicMock()
    mock_et_class.return_value.__enter__.return_value = mock_et_instance
    mock_et_instance.get_metadata.return_value = [{"XMP:Model": "...", "EXIF:Model": "", "EXIF:Make": "DJI"}]
  The mock_et_class.return_value is the instance; __enter__.return_value is what `et` resolves to.

Tests to write:

--- detect_from_exiftool ---
Cases:
  (a) Mini 4 Pro via XMP:
      XMP:Model="FC8282", EXIF:Model="", EXIF:Make="DJI"
      Expect: (platform="mini4pro", meta=dict)
  (b) M4E via XMP:
      XMP:Model="FC9100", EXIF:Model="", EXIF:Make="DJI"
      Expect: ("m4e", meta)
  (c) M3E via EXIF:Model (not XMP):
      XMP:Model="", EXIF:Model="DJI Mavic 3 Enterprise", EXIF:Make="DJI"
      Expect: ("m3e", meta)
  (d) Unrecognized model:
      XMP:Model="UNKNOWN", EXIF:Model="UNKNOWN", EXIF:Make="DJI"
      Expect: (None, meta) — function returns (None, meta) when nothing matches
  (e) exiftool ImportError path:
      mocker.patch("exiftool.ExifToolHelper", side_effect=ImportError)
      Actually: source does `import exiftool` inside the function; patch at origin.
      Use: mocker.patch.dict("sys.modules", {"exiftool": None}) or
           mocker.patch("builtins.__import__", side_effect that raises ImportError for "exiftool").
      Simpler approach: verify detect_from_exiftool returns (None, None) when
      `exiftool.ExifToolHelper.__init__` raises an exception (catch-all except covers this).
      Mock by having mock_et_class.return_value.__enter__ raise Exception("test").
      Expect: (None, None)

--- detect_from_exif (fallback chain) ---
Cases:
  (a) exiftool succeeds: patch detect_from_exiftool to return ("mini4pro", {}) via
      mocker.patch("platform_detect.detect_from_exiftool", return_value=("mini4pro", {})).
      Call detect_from_exif("fake.jpg"). Assert returns "mini4pro".
      Assert PIL.Image.open NOT called.
  (b) PIL fallback: patch detect_from_exiftool to return (None, None).
      Patch PIL.Image.open to return a mock with .getexif() returning {272: "FC8282"}.
      (Tag 272 = Model. Direct lookup in EXIF_MODEL_MAP: "FC8282" -> "mini4pro".)
      Assert detect_from_exif returns "mini4pro".
  (c) PIL fallback — M3E via pattern:
      patch detect_from_exiftool to return (None, None).
      Patch PIL.Image.open; getexif returns {272: "DJI Mavic 3E", 271: "DJI"}.
      Assert returns "m3e".
  (d) No EXIF data: getexif() returns None. Assert returns None.

--- detect_from_ffprobe ---
Cases:
  (a) Mini 4 Pro in format tags:
      mocker.patch("subprocess.run") returning CompletedProcess with returncode=0,
      stdout=json.dumps({"format": {"tags": {"encoder": "DJI Mini 4 Pro"}}, "streams": []}).
      Assert returns "mini4pro".
  (b) M4E in stream side_data:
      stdout=json.dumps({"format": {}, "streams": [{"tags": {"make": "DJI Matrice 4E"}}]})
      Assert returns "m4e".
  (c) returncode != 0: subprocess returns returncode=1. Assert returns None.
  (d) JSON decode error: subprocess returns returncode=0, stdout="not json". Assert returns None.
  (e) No matching patterns: stdout=json.dumps({"format": {"tags": {}}, "streams": []}).
      Assert returns None.

--- _extract_metadata_text ---
Cases:
  (a) Format tags only:
      data = {"format": {"tags": {"encoder": "DJI Mini 4 Pro", "artist": "test"}}, "streams": []}
      result = _extract_metadata_text(data)
      Assert "DJI Mini 4 Pro" in result
  (b) Stream tags:
      data = {"format": {}, "streams": [{"tags": {"make": "DJI Matrice 4E"}}]}
      Assert "DJI Matrice 4E" in result
  (c) Nested stream side_data:
      data = {"format": {}, "streams": [{"side_data_list": [{"some_tag": "M3E value"}]}]}
      Assert "M3E value" in result

--- detect_platform_from_folder (integration of detection chain, tmp_path) ---
Cases:
  (a) Photo EXIF path: Create tmp_path/photos/jpeg/DJI_0001.JPG (0-byte or fake content).
      Patch detect_from_exiftool to return ("m4e", {}) for any path.
      Patch detect_from_exif to return "m4e" (covers PIL fallback).
      Assert detect_platform_from_folder(str(tmp_path)) returns ("m4e", confidence, method)
      where confidence in ("confirmed", "likely") and method indicates EXIF.
  (b) No files at all: tmp_path is empty. Assert returns (None, "insufficient_data", ...) or similar.
      (Check source for exact return values on empty folder.)

Note on detect_platform_from_folder: read platform_detect.py lines 291-354 before writing this test.
The function returns (platform, confidence, method). Confidence values: "confirmed", "likely",
"filename_only", "insufficient_data". Patch at the right level — mock detect_from_exif for photo files.
  </behavior>
  <implementation>
    Replace stub contents wholesale. Keep `import platform_detect` at the top.
    Add imports: pytest, json, subprocess, unittest.mock.MagicMock.
    No importorskip guard needed (platform_detect.py has no module-level sys.exit()).
    Each test function must be independent — fresh mocks, no shared state.
    Use mocker fixture (pytest-mock) for all patches — auto-teardown.
    For detect_from_ffprobe tests, prefer direct mocker.patch("subprocess.run") over
    the conftest mock_ffmpeg fixture, since you need precise control of stdout JSON.
    For detect_platform_from_folder, read the source function before writing the test
    to know exact return structure and what sub-functions to mock.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: RED — Write failing tests for platform_detect.py (UNIT-02)</name>
  <files>tests/test_platform_detect.py, platform_detect.py</files>
  <action>
Before writing tests: read platform_detect.py lines 291-354 to understand detect_platform_from_folder
return structure. Also confirm _extract_metadata_text handles stream side_data by reading lines 222-260.

Write the complete test body for tests/test_platform_detect.py:
  Keep: `import platform_detect` (no importorskip needed).
  Add imports: pytest, json, subprocess, from unittest.mock import MagicMock
  Add function imports: from platform_detect import detect_from_exiftool, detect_from_exif, detect_from_ffprobe, _extract_metadata_text, detect_platform_from_folder

Write all tests described in the feature behavior section above.

CRITICAL: Apply the pyexiftool context manager pattern correctly:
  mock_et_class = mocker.patch("exiftool.ExifToolHelper")
  mock_et_instance = MagicMock()
  mock_et_class.return_value.__enter__.return_value = mock_et_instance
  mock_et_instance.get_metadata.return_value = [{...}]

After writing, run: pytest tests/test_platform_detect.py -v
These should PASS (source functions exist). If any test fails due to incorrect expected
values (e.g., wrong confidence string from detect_platform_from_folder), read the source
and fix the assertion to match actual behavior.
  </action>
  <verify>pytest tests/test_platform_detect.py -v 2>&amp;1 | tail -30</verify>
  <done>All tests in test_platform_detect.py pass. pytest exit code 0. No test_placeholder() remains. At least 12 distinct test functions covering exiftool, PIL fallback, ffprobe, metadata text extraction, and folder detection.</done>
</task>

</tasks>

<verification>
Run: pytest tests/test_platform_detect.py -v
Expected: All tests pass, exit code 0, no test_placeholder() remaining.

Run: pytest tests/test_platform_detect.py --co -q
Confirms test collection without import errors.

Run: pytest tests/ -v
Confirms no regressions in Phase 2 infrastructure or other test files.
</verification>

<success_criteria>
- pytest tests/test_platform_detect.py exits 0 with at least 12 passing tests
- detect_from_exiftool: Mini 4 Pro, M4E, M3E, unrecognized, and exception cases covered
- detect_from_exif: exiftool-success path, PIL fallback path, no-EXIF path covered
- detect_from_ffprobe: success (mini4pro), success (m4e), returncode!=0, JSON error, no match
- _extract_metadata_text: format tags, stream tags, side_data coverage
- detect_platform_from_folder: photo EXIF path with tmp_path files tested
- pyexiftool context manager pattern applied correctly (no AttributeError on get_metadata)
</success_criteria>

<output>
After completion, create .planning/phases/03-ingest-layer-tests/03-02-SUMMARY.md
</output>
