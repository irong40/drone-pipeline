---
phase: 03-ingest-layer-tests
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_ingest_sorter.py
  - tests/test_ingest.py
autonomous: true
requirements:
  - UNIT-01
  - UNIT-14

must_haves:
  truths:
    - "pytest tests/test_ingest_sorter.py passes with no failures or errors"
    - "pytest tests/test_ingest.py passes with no failures or errors"
    - "extract_sequence_number covers both timestamp and sequential filename branches plus None case"
    - "sort_by_sequence_ranges correctly assigns files to missions and populates unassigned list"
    - "build_mission_folder_name produces the correct SAI_M{N}_{pkg}_{date} format"
    - "validate_timestamp_gaps detects timestamp gaps exceeding the threshold and returns warning strings"
    - "scan_sd_card finds DJI files recursively and skips non-DJI files"
    - "copy_file_to_mission routes to correct subfolder and blocks path traversal attempts"
    - "parse_dji_filename parses M4E-format timestamps/sequences and returns None for non-DJI filenames"
    - "split_missions groups files into missions correctly and splits on gap_minutes threshold"
    - "gimbal_to_orientation returns correct 9-element matrix using pytest.approx for float comparison"
    - "get_utm_zone returns correct zone number and EPSG code for Hampton Roads longitude"
  artifacts:
    - path: "tests/test_ingest_sorter.py"
      provides: "UNIT-01 test coverage for ingest_sorter.py"
      min_lines: 120
    - path: "tests/test_ingest.py"
      provides: "UNIT-14 test coverage for ingest.py pure functions"
      min_lines: 80
  key_links:
    - from: "tests/test_ingest_sorter.py"
      to: "ingest_sorter.extract_sequence_number"
      via: "direct import"
      pattern: "from ingest_sorter import"
    - from: "tests/test_ingest.py"
      to: "ingest.gimbal_to_orientation"
      via: "direct import + pytest.approx"
      pattern: "from ingest import"
    - from: "tests/test_ingest_sorter.py"
      to: "requests.post"
      via: "mocker.patch in fire_webhook tests"
      pattern: "mocker.patch.*requests.post"
---

<objective>
Replace the stub test files for ingest_sorter.py and ingest.py with real unit tests covering all critical logic paths.

Purpose: Verify UNIT-01 (file sorting, sequence assignment, mission config parsing) and UNIT-14 (MipMap photogrammetry ingest logic) with deterministic, fast unit tests that never touch production paths or external services.

Output: Populated tests/test_ingest_sorter.py and tests/test_ingest.py with passing test suites.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ingest-layer-tests/03-RESEARCH.md
</context>

<feature>
  <name>ingest_sorter.py unit tests (UNIT-01)</name>
  <files>tests/test_ingest_sorter.py, ingest_sorter.py</files>
  <behavior>
Replace the test_placeholder() stub with real tests. Keep the pytest.importorskip("requests")
guard at the top — do not remove it.

Import only the specific functions under test (NOT main()):
  from ingest_sorter import (
      extract_sequence_number, detect_platform, sort_by_sequence_ranges,
      build_mission_folder_name, validate_timestamp_gaps, copy_file_to_mission,
      scan_sd_card, fire_webhook, count_inventory, create_mission_structure,
  )

Tests to write (function by function):

--- extract_sequence_number ---
Cases: input -> expected output
  "DJI_20260218101500_0015_D.JPG" -> 15    (timestamp branch)
  "DJI_20260218101500_0001_D.DNG" -> 1     (timestamp branch, seq 1)
  "DJI_0015.JPG"                  -> 15    (sequential branch)
  "DJI_0001.MP4"                  -> 1     (sequential branch, video)
  "RANDOM_0015.JPG"               -> None  (no DJI prefix)

--- detect_platform ---
Cases:
  "DJI_20260218101500_0015_D.JPG" -> "m4e"      (timestamp pattern = M4E/ambiguous)
  "DJI_0015.JPG"                  -> "mini4pro"  (sequential = Mini 4 Pro)
  "RANDOM_FILE.JPG"               -> None        (no match)

--- sort_by_sequence_ranges ---
Cases:
  (a) Two missions, files distributed:
      files = [seq1 JPG, seq5 JPG, seq10 JPG], missions = [{id:"uuid-1", start:1, end:6}, {id:"uuid-2", start:7, end:12}]
      expect sorted_m["uuid-1"] has seq1 and seq5, sorted_m["uuid-2"] has seq10, unassigned = []
  (b) Unassigned file:
      files = [seq20 JPG], missions = [{start:1, end:6}]
      expect sorted_m is empty for this file, unassigned has seq20 file

--- build_mission_folder_name ---
Cases:
  {"mission_number": 47, "package_type": "re_standard", "date": "20260218"} -> "SAI_M0047_re_standard_20260218"
  {"mission_number": 1, "package_type": "video", "date": "20260101"}        -> "SAI_M0001_video_20260101"

--- validate_timestamp_gaps ---
Cases:
  (a) Gap exceeds threshold (35 min > 30 min default):
      Use naive datetime objects (matching extract_timestamp behavior — strptime returns naive).
      Two files in the same mission 35 minutes apart -> expect 1 warning string containing "gap"
  (b) Gap within threshold (20 min < 30 min):
      No warnings returned.
  (c) Single file in mission (< 2 timestamp files):
      No warnings (not enough files to compute gap).

--- scan_sd_card (tmp_path) ---
Cases:
  (a) Flat structure: Create DJI_0001.JPG, DJI_0002.JPG, non_dji.txt in tmp_path.
      Expect 2 files returned, all have extension "JPG".
  (b) Recursive structure: Create tmp_path/subdir/DJI_0003.MP4.
      Expect the file found (rglob is recursive).
  (c) File with no parseable sequence (DJI_BADNAME.JPG): excluded (seq is None).

--- copy_file_to_mission (tmp_path) ---
Cases:
  (a) Valid JPG -> routes to photos/jpeg subfolder. Use str(tmp_path / "source/DJI_0001.JPG")
      as path, and str(tmp_path / "mission") as mission_path. Create source file first.
      Create photos/jpeg subfolder in mission_path. Assert returned dest_path exists.
  (b) Unknown extension (e.g. "XML") -> returns None (no routing entry).
  (c) Path traversal blocked: file_info with filename containing "../escape.jpg" ->
      copy_file_to_mission returns None. Note: use os.path.basename behavior — safe_name
      will strip the traversal, but dest_path check will still validate.
      Better: pass a filename like "DJI_0001.JPG" but construct a file_info["path"] that is
      outside tmp_path. Verify the startswith guard fires.

--- fire_webhook ---
Cases:
  (a) Successful POST: mocker.patch("requests.post") returns mock with status_code=200,
      raise_for_status does nothing. Call fire_webhook({...mission_config}, {...inventory}, "mini4pro").
      Assert requests.post called once. Assert returned True.
      Verify payload includes "ingested_at" key with "Z" suffix (not "+00:00").
  (b) RequestException: mock raise_for_status to raise requests.RequestException.
      Assert fire_webhook returns False.

--- count_inventory (tmp_path) ---
  Create photos/jpeg/DJI_0001.JPG, video/full/DJI_0001.MP4, ppk/DJI_0001.MRK in tmp_path.
  Assert photo_count=1, video_count=1, has_ppk_data=True.
  </behavior>
  <implementation>
    Replace the stub file contents wholesale. Keep the importorskip guard and the `import ingest_sorter`
    import at the top. Add all imports: pytest, unittest.mock.MagicMock, datetime, os.
    All tests use function-scoped state (no shared fixtures except mocker from pytest-mock).
    Use `str(tmp_path / ...)` paths for all file I/O tests to stay within pytest tmp dirs.
    For copy_file_to_mission: create the destination subfolder before calling the function,
    since copy_file_to_mission does NOT create subdirs — it just copies.
    Actually: check the source — copy_file_to_mission calls shutil.copy2, which requires
    dest_dir to exist. Create it: os.makedirs(os.path.join(mission_path, "photos/jpeg"), exist_ok=True).
  </implementation>
</feature>

<feature>
  <name>ingest.py unit tests (UNIT-14)</name>
  <files>tests/test_ingest.py, ingest.py</files>
  <behavior>
Replace the test_placeholder() stub with real tests. Keep the pytest.importorskip("PIL")
guard and `import ingest` at the top.

Import only the specific functions under test:
  from ingest import (
      parse_dji_filename, split_missions, get_utm_zone,
      gimbal_to_orientation, extract_gps_from_exif, extract_xmp_gimbal,
  )

Tests to write:

--- parse_dji_filename ---
Cases:
  "DJI_20260218101500_0015_D.JPG" -> dict with datetime(2026,2,18,10,15,0), sequence=15, extension="JPG"
  "DJI_20260218101500_0001_D_NOV25.DNG" -> sequence=1, extension="DNG"
  "DJI_0015.JPG" -> None  (Mini 4 Pro sequential format, not M4E format)
  "random.jpg"   -> None  (no DJI prefix)

--- split_missions ---
Cases:
  (a) Two missions separated by 35-minute gap (> 30 min default):
      Build list of file dicts with datetime objects. 3 files in mission 1, 2 files
      35 minutes later in mission 2. Assert len(result) == 2.
  (b) All files within gap threshold: Assert len(result) == 1.
  (c) Empty files list: Assert result == {}.

--- get_utm_zone ---
Cases:
  longitude=-76.3 (Hampton Roads VA) -> zone=18, epsg=32618
  longitude=-180.0 (west edge)       -> zone=1, epsg=32601
  longitude=0.0 (prime meridian)     -> zone=31, epsg=32631

--- gimbal_to_orientation ---
Use pytest.approx(expected, abs=1e-9) for all float comparisons.
Cases:
  (a) Zero gimbal (pitch=0, roll=0, yaw=0):
      Expected: [1, 0, 0, 0, 0, -1, 0, 1, 0]  (standard DJI nadir orientation)
      Note: verify against the formula in source — Row1[2]=-cos(0)=-1, Row2[2]=sin(0)=0
  (b) 90-degree yaw (pitch=0, roll=0, yaw=90):
      cos(90°)≈0, sin(90°)≈1
      Row0: [0, -1, 0], Row1: [0, 0, -1], Row2: [1, 0, 0]
      Expected: [0, -1, 0, 0, 0, -1, 1, 0, 0]
  (c) Non-zero roll triggers the full Rz@Rx@Ry path (roll=1.0 deg):
      Just verify it returns a 9-element list (not the simplified formula path).
      Assert len(result) == 9.

--- extract_gps_from_exif (mock PIL) ---
Cases:
  (a) Valid GPS EXIF:
      mock_img._getexif returns {34853: {1:"N", 2:(37,0,0), 3:"W", 4:(76,0,0), 6:50.0}}
      patch "PIL.Image.open" to return mock_img.
      Assert result == [lon, lat, alt] -> [-76.0, 37.0, 50.0]
  (b) No EXIF (getexif returns None): Assert result is None.
  (c) No GPS tag in EXIF: Assert result is None.

--- extract_xmp_gimbal (tmp_path) ---
Cases:
  (a) Write a fake XMP block into a tmp file containing:
      drone-dji:GimbalPitchDegree="-90.0" drone-dji:GimbalRollDegree="0.0"
      drone-dji:GimbalYawDegree="45.0" drone-dji:RelativeAltitude="30.5"
      drone-dji:AbsoluteAltitude="35.2"
      Wrap in <x:xmpmeta>...</x:xmpmeta> tags.
      Assert result["pitch"] == -90.0 and result["yaw"] == 45.0.
  (b) File with no XMP block: Assert result is None.
  </behavior>
  <implementation>
    Replace stub contents. Keep the importorskip("PIL") guard.
    Use mocker fixture for PIL.Image.open patches (pytest-mock auto-teardown).
    For extract_xmp_gimbal: write test bytes to tmp_path file using tmp_path.joinpath("test.jpg").write_bytes().
    The XMP block must start with b"<x:xmpmeta" and end with b"</x:xmpmeta>".
    Use realistic but minimal XMP: b'<x:xmpmeta drone-dji:GimbalPitchDegree="-90.0" drone-dji:GimbalRollDegree="0.0" drone-dji:GimbalYawDegree="45.0" drone-dji:RelativeAltitude="30.5" drone-dji:AbsoluteAltitude="35.2"></x:xmpmeta>'
    For gimbal zero test: verify formula against source code comments to get exact expected values.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: RED — Write failing tests for ingest_sorter.py (UNIT-01) and ingest.py (UNIT-14)</name>
  <files>tests/test_ingest_sorter.py, tests/test_ingest.py</files>
  <action>
Write the complete test bodies for both files, replacing the test_placeholder() functions.

For tests/test_ingest_sorter.py:
  Keep: pytest.importorskip("requests") guard + import ingest_sorter.
  Add imports: from datetime import datetime; import os; from unittest.mock import MagicMock; import requests
  Add function imports: from ingest_sorter import extract_sequence_number, detect_platform, sort_by_sequence_ranges, build_mission_folder_name, validate_timestamp_gaps, copy_file_to_mission, scan_sd_card, fire_webhook, count_inventory
  Write all tests described in the feature behavior section above.

For tests/test_ingest.py:
  Keep: pytest.importorskip("PIL") guard + import ingest.
  Add imports: import math; import pytest; from datetime import datetime; from unittest.mock import MagicMock
  Add function imports: from ingest import parse_dji_filename, split_missions, get_utm_zone, gimbal_to_orientation, extract_gps_from_exif, extract_xmp_gimbal
  Write all tests described in the feature behavior section above.

After writing, run: pytest tests/test_ingest_sorter.py tests/test_ingest.py -v
These should PASS immediately because the source functions already exist and are correct.
If any test fails due to incorrect expected values, fix the test assertion to match the actual function output before committing.
  </action>
  <verify>pytest tests/test_ingest_sorter.py tests/test_ingest.py -v 2>&amp;1 | tail -30</verify>
  <done>All tests in both files pass. pytest exit code 0. No test_placeholder() functions remain. Each file has at least 8 distinct test functions beyond the importorskip guard.</done>
</task>

</tasks>

<verification>
Run: pytest tests/test_ingest_sorter.py tests/test_ingest.py -v
Expected: All tests pass, exit code 0, no test_placeholder() remaining.

Run: pytest tests/test_ingest_sorter.py tests/test_ingest.py --co -q
Confirms test collection works without import errors.

Run: pytest tests/ -v
Confirms no regressions in other test files from Phase 2 infrastructure.
</verification>

<success_criteria>
- pytest tests/test_ingest_sorter.py exits 0 with at least 10 passing tests
- pytest tests/test_ingest.py exits 0 with at least 8 passing tests
- extract_sequence_number: both filename branches + None case tested
- sort_by_sequence_ranges: assigned + unassigned cases tested
- build_mission_folder_name: format verified
- validate_timestamp_gaps: gap-exceeds + gap-within + single-file cases
- scan_sd_card: flat + recursive + excluded-file cases
- copy_file_to_mission: correct routing verified with tmp_path
- fire_webhook: success + failure cases, payload "Z" suffix verified
- parse_dji_filename: M4E format + None cases
- split_missions: two-mission split + single mission + empty
- gimbal_to_orientation: zero-gimbal case with pytest.approx
- extract_gps_from_exif: valid GPS + no EXIF mocked with PIL.Image.open
- extract_xmp_gimbal: valid XMP bytes + no-XMP cases with tmp_path
</success_criteria>

<output>
After completion, create .planning/phases/03-ingest-layer-tests/03-01-SUMMARY.md
</output>
