---
phase: 12-integration-and-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - n8n/path_e_workflow.json
  - n8n/package_router_patch.json
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-07

must_haves:
  truths:
    - "n8n workflow fires when mission.vegetation_analysis=true AND Path C ortho exists"
    - "Workflow runs E1→E2→E3→E4 in sequence and pauses at webhook wait node for operator review"
    - "Package router enables vegetation by default for site_survey and environmental_survey"
    - "All 4 E scripts follow v1 contract: argparse, setup_logging, JSON stdout, exit codes, processing_steps"
  artifacts:
    - path: "n8n/path_e_workflow.json"
      provides: "n8n Path E workflow definition"
      contains: "veg_canopy_detection"
    - path: "n8n/package_router_patch.json"
      provides: "Package router update for vegetation trigger"
      contains: "vegetation_analysis"
  key_links:
    - from: "n8n workflow E0"
      to: "missions.vegetation_analysis check"
      via: "Supabase query node"
      pattern: "vegetation_analysis"
    - from: "n8n workflow review gate"
      to: "webhook wait node"
      via: "POST /sentinel-vegetation-resume"
      pattern: "sentinel-vegetation-resume"
---

<objective>
Create n8n Path E workflow definition, update package router for vegetation triggers, and verify all 4 E scripts meet v1 pipeline contract.

Purpose: n8n orchestrates the full Path E sequence. The package router decides which missions get vegetation analysis. Contract compliance ensures E scripts integrate cleanly with the existing pipeline infrastructure.
Output: n8n workflow JSON, package router patch, contract smoke test.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PRD-vegetation-analysis.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: n8n Path E workflow JSON</name>
  <files>n8n/path_e_workflow.json</files>
  <action>
Create n8n workflow JSON for Path E. Nodes:

1. **E0 — Check Ortho:** Supabase query for mission record. Check vegetation_analysis=true. Check Path C output exists (poll every 60s, max 30 attempts, timeout after 30min). If both true: continue. If ortho missing after timeout: set vegetation_status='failed', stop.

2. **Set Status — Detecting:** Patch missions.vegetation_status='detecting'. Insert 4 processing_steps rows (veg_canopy_detection, veg_species_classification, veg_health_assessment, veg_report_generation) all status='waiting'.

3. **E1 — Canopy Detection:** Execute node: `python E:\Sentinel\Scripts\canopy_detection.py --mission-id {{$json.mission_id}} --ortho-path {{$json.ortho_path}} --processing-job-id {{$json.processing_job_id}}`. Parse JSON stdout. If canopy_count=0: skip to E4 with "no vegetation" note.

4. **Set Status — Classifying:** Patch vegetation_status='classifying'.

5. **E2 — Species Classification:** Execute with --max-canopies from vegetation_config. Parse stdout.

6. **Set Status — Assessing:** Patch vegetation_status='assessing'.

7. **E3 — Health Assessment:** Execute with --vision-sample-pct from vegetation_config. Parse stdout.

8. **Set Status — Generating Report:** Patch vegetation_status='generating_report'.

9. **E4 — Report Generation:** Execute with --tier from mission config. Parse stdout for file paths.

10. **Set Status — Review:** Patch vegetation_status='review'.

11. **Review Gate — Webhook Wait:** Wait for POST to /sentinel-vegetation-resume with decisions array. Timeout: 7 days.

12. **Process Decisions:** Apply exclude/approve/flag decisions. If any excluded: re-run E4 to regenerate report. Patch vegetation_status='complete'.

Error handling: On any E script exit code 1, set vegetation_status='failed', log error, send notification. Exit code 2 (partial): continue but flag for review.

mkdir -p n8n/ before writing.
  </action>
  <verify>
    <automated>python -c "import json; json.load(open('n8n/path_e_workflow.json')); print('valid JSON')"</automated>
    <manual>Import into n8n and verify node connections</manual>
  </verify>
  <done>n8n Path E workflow JSON with E0-E4 sequence, review gate, error handling</done>
</task>

<task type="auto">
  <name>Task 2: Package router patch + v1 contract smoke test</name>
  <files>n8n/package_router_patch.json</files>
  <action>
**Package router update:**
Create patch document showing what to change in the existing package router workflow:
- site_survey template: vegetation_enabled=true by default
- environmental_survey template: vegetation_enabled=true by default
- construction_hybrid template: vegetation_enabled=false (optional, operator toggles)
- Routing condition: IF mission.vegetation_analysis=true AND Path C status='complete' THEN fire Path E

**v1 contract smoke test (inline verification):**
For each E script (canopy_detection.py, species_classification.py, health_assessment.py, vegetation_report.py), verify:
1. `python script.py --help` exits 0 (argparse configured)
2. Script imports setup_logging (grep for LOG_DIR)
3. Script imports PipelineStatusReporter (grep for pipeline_status)
4. Script has `json.dumps(result)` for stdout output
5. Script has sys.exit(0) and sys.exit(1) patterns
6. Script imports from checkpoint module

Write results as a checklist in the SUMMARY.
  </action>
  <verify>
    <automated>python -c "import json; json.load(open('n8n/package_router_patch.json')); print('valid')"</automated>
    <manual>Verify all 4 E scripts pass contract checks</manual>
  </verify>
  <done>Package router enables vegetation for site/environmental surveys; all E scripts pass v1 contract</done>
</task>

</tasks>

<verification>
1. n8n workflow JSON is valid and importable
2. Package router correctly routes vegetation missions
3. All 4 E scripts pass v1 contract smoke test
</verification>

<success_criteria>
- INT-01, INT-02, INT-07 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/12-integration-and-delivery/12-01-SUMMARY.md`
</output>
