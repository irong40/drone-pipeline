---
phase: 06-integration-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/integration/test_video_pipeline.py
autonomous: true
requirements:
  - INTG-02
must_haves:
  truths:
    - "color grade step creates graded output files in video/graded/ with _graded suffix"
    - "checkpoint file is written to mission folder after successful processing"
    - "proxy generation step creates proxy output files in video/proxy/ after grading"
    - "scripts skip already-processed files when run a second time (checkpoint resume)"
    - "FFmpeg is never actually invoked — subprocess.run is mocked throughout"
  artifacts:
    - path: "tests/integration/test_video_pipeline.py"
      provides: "End-to-end video pipeline integration test — 4 tests"
---

<objective>
Integration test for the video pipeline flow: color grading (V1) through proxy generation (V4)
against a real mission folder structure with mocked FFmpeg.

Purpose: Verify two-step video pipeline (grade -> proxy) works end-to-end with real directory
structures and checkpoint files, without requiring actual FFmpeg or GPU hardware.
Output: tests/integration/test_video_pipeline.py with 4 integration tests.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/redle/drone-pipeline/video_color_grade.py
@C:/Users/redle/drone-pipeline/video_proxy_gen.py
@C:/Users/redle/drone-pipeline/checkpoint.py
@C:/Users/redle/drone-pipeline/tests/conftest.py
@C:/Users/redle/drone-pipeline/tests/test_video_color_grade.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write video pipeline integration test</name>
  <files>tests/integration/test_video_pipeline.py</files>
  <action>
    Create tests/integration/test_video_pipeline.py. FFmpeg subprocess calls mocked;
    all directory and checkpoint I/O is real against tmp_path.

    Supabase stub (autouse, same pattern as test_video_color_grade.py).

    FIXTURE mission_folder(tmp_path): standard mission structure:
      video/full/ — DJI_0001.MP4, DJI_0002.MP4 (fake content)
      video/graded/ — empty
      video/proxy/ — empty
      LUTs/ — Sentinel_DCinelike.cube (empty bytes)

    FIXTURE mock_ffmpeg_success(mocker): mock subprocess.run with side_effect
    that writes fake output to the last cmd arg (output path), returns rc=0.

    TEST test_color_grade_creates_graded_files:
      get_lut_path -> find_videos -> grade_video for each -> save_checkpoint
      Assert graded files exist in video/graded/, checkpoint file written.

    TEST test_color_grade_checkpoint_skips_completed:
      Pre-populate checkpoint with DJI_0001.MP4. Run grade loop.
      Assert subprocess.run called only once (for DJI_0002.MP4).

    TEST test_proxy_generation_runs_after_grading:
      Pre-populate video/graded/ with fake graded files.
      Mock subprocess.run for proxy ffmpeg call.
      Assert proxy files exist in video/proxy/.

    TEST test_video_pipeline_no_videos_exits_cleanly:
      Remove all files from video/full/.
      find_videos returns empty. No subprocess.run calls made.
  </action>
  <verify>python -m pytest tests/integration/test_video_pipeline.py -v — 4 passed</verify>
  <done>All 4 tests pass. Checkpoint files physically verified.</done>
</task>

</tasks>

<verification>
  python -m pytest tests/integration/test_video_pipeline.py -v
</verification>

<success_criteria>
- 4 tests passing
- Checkpoint file written to mission folder (real I/O)
- Graded outputs exist in video/graded/
- Checkpoint skip logic verified
- subprocess.run never called with real FFmpeg
</success_criteria>

<output>
After completion, create .planning/phases/06-integration-tests/06-02-SUMMARY.md
</output>
