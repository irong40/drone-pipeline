---
phase: 01-code-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - video_color_grade.py
autonomous: true
requirements:
  - GAP-10

must_haves:
  truths:
    - "After video_color_grade.py successfully grades a clip, the video_assets row for that file in Supabase has graded_path set to the output file path"
    - "Grading works fully without --upload or --mission-id — Supabase update is opt-in"
    - "If SUPABASE_URL or SUPABASE_SERVICE_KEY env vars are not set, grading continues normally and Supabase update is silently skipped"
    - "If upsert fails (network error, constraint issue), grading result is not affected — error is logged as warning only"
    - "video_color_grade.py accepts --upload and --mission-id CLI arguments"
  artifacts:
    - path: "video_color_grade.py"
      provides: "update_graded_path() function + --upload + --mission-id args"
      contains: "update_graded_path"
      exports:
        - "update_graded_path"
  key_links:
    - from: "video_color_grade.py grade_video() success branch"
      to: "update_graded_path()"
      via: "if ok: ... if args.upload and args.mission_id:"
      pattern: "args\\.upload.*args\\.mission_id"
    - from: "update_graded_path()"
      to: "supabase video_assets table"
      via: "client.table('video_assets').upsert(on_conflict='mission_id,filename')"
      pattern: "upsert.*on_conflict.*mission_id,filename"
---

<objective>
Add Supabase graded_path update to video_color_grade.py so color grading metadata is persisted immediately without requiring a separate video_metadata run.

Purpose: Currently graded_path is never written to video_assets — the column stays NULL until video_metadata.py runs. This means proxy gen, QA, and delivery scripts cannot reliably find graded clips via Supabase queries. After this plan, the graded path is written atomically with the grade operation.
Output: video_color_grade.py with update_graded_path() function + --upload/--mission-id CLI args.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-hardening/01-RESEARCH.md
@.planning/phases/01-code-hardening/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify video_assets unique constraint and add update_graded_path() to video_color_grade.py</name>
  <files>
    video_color_grade.py
  </files>
  <action>
**Step 1 — Verify the Supabase unique constraint (required before upsert will work):**

Run this to check if the UNIQUE constraint on video_assets(mission_id, filename) exists:

  python -c "
import os
url = os.environ.get('SUPABASE_URL', '')
key = os.environ.get('SUPABASE_SERVICE_KEY', '')
if not url or not key:
    print('SKIP: SUPABASE_URL or SUPABASE_SERVICE_KEY not set')
else:
    from supabase import create_client
    client = create_client(url, key)
    # Try a test upsert with a dummy mission_id to check constraint
    try:
        client.table('video_assets').upsert(
            {'mission_id': '00000000-0000-0000-0000-000000000000', 'filename': '__constraint_check__'},
            on_conflict='mission_id,filename'
        ).execute()
        # Clean up the test row
        client.table('video_assets').delete().eq('filename', '__constraint_check__').execute()
        print('UNIQUE constraint EXISTS — upsert is safe')
    except Exception as e:
        if 'unique' in str(e).lower() or 'constraint' in str(e).lower():
            print(f'UNIQUE constraint MISSING — need migration: {e}')
        else:
            print(f'Other error (may be RLS): {e}')
"

If the constraint is MISSING: Use the two-step update approach instead of upsert — do a SELECT first, then UPDATE if row exists, INSERT if not. The two-step approach does not require a unique constraint:

  # Two-step fallback (use if constraint is missing):
  existing = client.table("video_assets").select("id").eq("mission_id", mission_id).eq("filename", filename).execute()
  if existing.data:
      client.table("video_assets").update({"graded_path": graded_path}).eq("id", existing.data[0]["id"]).execute()
  else:
      client.table("video_assets").insert({"mission_id": mission_id, "filename": filename, "graded_path": graded_path}).execute()

If the constraint EXISTS: Use the clean upsert path from the research doc.

**Step 2 — Add SUPABASE_URL and SUPABASE_SERVICE_KEY to the CONFIG section:**

video_color_grade.py currently has no Supabase config. Add after the existing constants:

  SUPABASE_URL = os.environ.get("SUPABASE_URL", "")
  SUPABASE_SERVICE_KEY = os.environ.get("SUPABASE_SERVICE_KEY", "")

**Step 3 — Add update_graded_path() function** (after the CONFIG section, before the LOGGING section or in its own section):

  def update_graded_path(mission_id, filename, graded_path):
      """Update video_assets.graded_path after successful color grading.

      Uses upsert so this is safe to call even if the row does not yet exist
      (video_metadata.py may not have run yet).
      Returns True on success, False on failure (failure is non-fatal).
      """
      if not SUPABASE_URL or not SUPABASE_SERVICE_KEY:
          return False
      try:
          from supabase import create_client
          client = create_client(SUPABASE_URL, SUPABASE_SERVICE_KEY)
          client.table("video_assets").upsert(
              {
                  "mission_id": mission_id,
                  "filename": filename,
                  "graded_path": graded_path,
              },
              on_conflict="mission_id,filename",
          ).execute()
          return True
      except Exception as e:
          logging.getLogger(__name__).warning(f"Supabase graded_path update failed: {e}")
          return False

  (Use the two-step fallback version instead if the constraint check in Step 1 showed it is missing.)

**Step 4 — Add CLI arguments to argparse in main():**

After the existing --codec argument:
  parser.add_argument("--mission-id", help="Supabase mission UUID (required for --upload)")
  parser.add_argument("--upload", action="store_true",
                      help="Update graded_path in Supabase video_assets after each successful grade")

**Step 5 — Call update_graded_path() in the processing loop**, inside the `if ok:` branch, after the existing log.info and results.append lines:

  if ok:
      size_mb = os.path.getsize(output_path) / (1024 * 1024)
      log.info(f"  OK: {output_path} ({size_mb:.1f} MB)")
      results.append({"input": video_path, "output": output_path, "status": "ok"})
      # GAP-10: update Supabase graded_path
      if args.upload and args.mission_id:
          updated = update_graded_path(args.mission_id, filename, output_path)
          if updated:
              log.info(f"  Supabase: graded_path updated")
          # else: warning already logged in update_graded_path()
      elif args.upload and not args.mission_id:
          log.warning("  --upload requires --mission-id; skipping Supabase update")

Note: `filename` is already set earlier in the loop as `filename = os.path.basename(video_path)`. Use it directly.
  </action>
  <verify>
1. Constraint check output from Step 1 is captured — note the result for SUMMARY.md.

2. Structural checks:
   grep -n "SUPABASE_URL\|SUPABASE_SERVICE_KEY\|update_graded_path\|--mission-id\|--upload" video_color_grade.py

   Expected: at least 6 matches covering config constants, function definition, argparse args, and call site.

3. Dry-run compatibility (no Supabase env needed):
   python video_color_grade.py --help 2>/dev/null | grep -E "mission-id|upload"
   Expected: both --mission-id and --upload appear in help output.

4. When SUPABASE_URL is not set, skip is silent:
   python -c "
import video_color_grade as vcg
result = vcg.update_graded_path('test-id', 'test.mp4', '/path/graded.mp4')
assert result == False, f'Expected False when no env vars, got {result}'
print('Non-fatal skip: OK')
"
  </verify>
  <done>
video_color_grade.py has SUPABASE_URL/KEY constants, update_graded_path() function, --upload and --mission-id CLI args, and the function is called in the grade loop after each success. Grading without --upload continues to work exactly as before. Supabase failures are non-fatal warnings.
  </done>
</task>

</tasks>

<verification>
1. grep -n "update_graded_path\|--upload\|--mission-id\|SUPABASE_URL" video_color_grade.py → shows all 4 components present
2. python video_color_grade.py --help | grep upload → "--upload" appears in help
3. python video_color_grade.py --help | grep mission-id → "--mission-id" appears in help
4. python -c "import video_color_grade; r = video_color_grade.update_graded_path('x','y','z'); assert r == False" → passes (no Supabase env set)
5. Unique constraint check result logged in SUMMARY.md
</verification>

<success_criteria>
- update_graded_path() is callable from outside (can be imported and tested in Phase 4 unit tests)
- Grading with --upload --mission-id UUID writes graded_path to Supabase video_assets
- Grading without --upload works identically to before this change
- Supabase failures do not abort grading — logged as warning, processing continues
- SUMMARY.md records whether the unique constraint exists (for Phase 4 test planning)
</success_criteria>

<output>
After completion, create .planning/phases/01-code-hardening/01-04-SUMMARY.md

Note in the SUMMARY.md:
- Whether the UNIQUE constraint on video_assets(mission_id, filename) exists (constraint check result from Task 1 Step 1)
- Whether upsert or two-step update was implemented (affects Phase 4 unit test approach)
</output>
