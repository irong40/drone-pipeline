---
phase: 01-code-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - video_color_grade.py
  - video_proxy_gen.py
  - video_format_export.py
  - srt_telemetry_parser.py
  - video_qa.py
  - archive_sync.py
  - ingest_sorter.py
  - folder_watcher.py
autonomous: true
requirements:
  - GAP-13
  - DEPR-01

must_haves:
  truths:
    - "All 5 video scripts write to E:\\Sentinel\\logs\\{script_name}.log in addition to stdout"
    - "Running python -W error on archive_sync.py, ingest_sorter.py, and folder_watcher.py produces no DeprecationWarning for datetime"
    - "Log directory is created automatically if it does not exist"
  artifacts:
    - path: "video_color_grade.py"
      provides: "File + stdout logging with LOG_DIR constant"
      contains: "LOG_DIR"
    - path: "video_proxy_gen.py"
      provides: "File + stdout logging with LOG_DIR constant"
      contains: "LOG_DIR"
    - path: "video_format_export.py"
      provides: "File + stdout logging with LOG_DIR constant"
      contains: "LOG_DIR"
    - path: "srt_telemetry_parser.py"
      provides: "File + stdout logging with LOG_DIR constant"
      contains: "LOG_DIR"
    - path: "video_qa.py"
      provides: "File + stdout logging with LOG_DIR constant"
      contains: "LOG_DIR"
    - path: "archive_sync.py"
      provides: "datetime.now(datetime.UTC) replacing utcnow(); aware comparison on line 213"
      contains: "datetime.UTC"
    - path: "ingest_sorter.py"
      provides: "datetime.now(datetime.UTC) replacing utcnow() in fire_webhook()"
      contains: "datetime.UTC"
    - path: "folder_watcher.py"
      provides: "datetime.now(datetime.UTC) replacing utcnow() in build_inventory()"
      contains: "datetime.UTC"
  key_links:
    - from: "video_color_grade.py setup_logging()"
      to: "E:\\Sentinel\\logs\\video_color_grade.log"
      via: "logging.FileHandler"
      pattern: "FileHandler.*video_color_grade\\.log"
    - from: "archive_sync.py line ~206"
      to: "datetime.now(datetime.UTC)"
      via: "replace utcnow()"
      pattern: "datetime\\.now\\(datetime\\.UTC\\)"
    - from: "archive_sync.py line ~213"
      to: "timezone-aware comparison"
      via: "remove .replace(tzinfo=None)"
      pattern: "fromisoformat.*\\+00:00"
---

<objective>
Add persistent file logging to all 5 video pipeline scripts and fix 3 datetime deprecation sites.

Purpose: Video script output is lost when stdout is discarded by n8n orchestration. Persistent log files in E:\Sentinel\logs\ capture all output regardless of how the script is invoked. The datetime fixes eliminate active DeprecationWarnings on Python 3.14.3.
Output: 8 modified scripts — 5 with dual file+stdout logging, 3 with timezone-aware datetime.
</objective>

<execution_context>
@C:/Users/redle/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/redle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-code-hardening/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add file logging to 5 video scripts (GAP-13)</name>
  <files>
    video_color_grade.py
    video_proxy_gen.py
    video_format_export.py
    srt_telemetry_parser.py
    video_qa.py
  </files>
  <action>
In each of the 5 scripts, make two targeted changes to setup_logging():

1. Add LOG_DIR constant near the top of the CONFIG section (after existing constants):
   LOG_DIR = r"E:\Sentinel\logs"

2. Replace the existing setup_logging() body with the dual-handler version. The current body has only a StreamHandler. Add FileHandler alongside it:

   def setup_logging(log_dir=LOG_DIR):
       os.makedirs(log_dir, exist_ok=True)
       log_file = os.path.join(log_dir, "{script_name}.log")
       logging.basicConfig(
           level=logging.INFO,
           format="%(asctime)s [%(levelname)s] %(message)s",
           handlers=[
               logging.FileHandler(log_file),
               logging.StreamHandler(sys.stdout),
           ],
       )
       return logging.getLogger(__name__)

Log file name per script (replace {script_name} accordingly):
- video_color_grade.py   → "video_color_grade.log"
- video_proxy_gen.py     → "video_proxy_gen.log"
- video_format_export.py → "video_format_export.log"
- srt_telemetry_parser.py → "srt_telemetry_parser.log"
- video_qa.py            → "video_qa.log"

Critical: Do NOT add force=True to basicConfig. Do NOT restructure the function signature — it is called as setup_logging() in main() in all scripts and must remain callable with no arguments.

All 5 scripts already import os, sys, and logging. No new imports needed.

Reference pattern: ingest_sorter.py lines 73-84 (existing working implementation — copy this verbatim, changing only the log filename).
  </action>
  <verify>
For each modified script, grep confirms the pattern:
  grep -n "FileHandler\|LOG_DIR" video_color_grade.py video_proxy_gen.py video_format_export.py srt_telemetry_parser.py video_qa.py

Each script should show:
- One line with LOG_DIR = r"E:\Sentinel\logs"
- One line with logging.FileHandler

Functional check (creates log dir and file):
  python -c "import video_color_grade; video_color_grade.setup_logging()" 2>/dev/null
  (No error = pass; log dir is created by os.makedirs)
  </verify>
  <done>
All 5 scripts have LOG_DIR constant and FileHandler in setup_logging(). grep for "FileHandler" returns 5 matches across the 5 files. Function signature unchanged — setup_logging() callable with no args.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix datetime.utcnow() deprecation in 3 files (DEPR-01)</name>
  <files>
    archive_sync.py
    ingest_sorter.py
    folder_watcher.py
  </files>
  <action>
Make the following targeted line replacements in each file. All 3 files already import `from datetime import datetime` (or `from datetime import datetime, timedelta`) — no import changes needed. `datetime.UTC` is a class attribute of datetime, available since Python 3.11.

**archive_sync.py — two changes:**

Line ~206 (in cleanup_old_delivered() or similar):
  BEFORE: cutoff = datetime.utcnow() - timedelta(days=days)
  AFTER:  cutoff = datetime.now(datetime.UTC) - timedelta(days=days)

Line ~213 (the Drive createdTime comparison — currently strips tzinfo to compare with naive utcnow):
  BEFORE: created = datetime.fromisoformat(f["createdTime"].replace("Z", "+00:00")).replace(tzinfo=None)
  AFTER:  created = datetime.fromisoformat(f["createdTime"].replace("Z", "+00:00"))

  (Remove .replace(tzinfo=None). Both cutoff and created are now timezone-aware; the comparison works correctly.)

**ingest_sorter.py — one change:**

Line ~339 (in fire_webhook() or payload construction):
  BEFORE: "ingested_at": datetime.utcnow().isoformat() + "Z",
  AFTER:  "ingested_at": datetime.now(datetime.UTC).isoformat().replace("+00:00", "Z"),

**folder_watcher.py — one change:**

Line ~109 (in build_inventory() or similar):
  BEFORE: "detected_at": datetime.utcnow().isoformat() + "Z",
  AFTER:  "detected_at": datetime.now(datetime.UTC).isoformat().replace("+00:00", "Z"),

Read each file first to confirm exact line numbers before editing. The line numbers above are approximate (from research grep — verify against actual file content).
  </action>
  <verify>
Run with deprecation-as-error flag for each file. Use python -W error to promote DeprecationWarnings to errors:

  python -W error -c "import archive_sync" 2>&1 | grep -i deprecation
  python -W error -c "import ingest_sorter" 2>&1 | grep -i deprecation
  python -W error -c "import folder_watcher" 2>&1 | grep -i deprecation

Each command should produce no output (no DeprecationWarning).

Also verify the archive_sync comparison is timezone-consistent:
  grep -n "fromisoformat\|replace(tzinfo" archive_sync.py
  (Should NOT contain .replace(tzinfo=None) on the createdTime line)
  </verify>
  <done>
All 3 files pass python -W error import without DeprecationWarning. archive_sync.py comparison is timezone-aware on both sides (no .replace(tzinfo=None) stripping the Drive timestamp). The Z-suffix format is preserved for n8n webhook payloads in ingest_sorter.py and folder_watcher.py.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. grep -rn "utcnow" archive_sync.py ingest_sorter.py folder_watcher.py → zero matches
2. grep -rn "FileHandler" video_color_grade.py video_proxy_gen.py video_format_export.py srt_telemetry_parser.py video_qa.py → 5 matches (one per file)
3. grep -rn "LOG_DIR" video_color_grade.py video_proxy_gen.py video_format_export.py srt_telemetry_parser.py video_qa.py → 5 matches
4. python -W error -c "import archive_sync; import ingest_sorter; import folder_watcher" → no DeprecationWarning output
</verification>

<success_criteria>
- 5 video scripts each write to E:\Sentinel\logs\{name}.log when run in production
- 0 occurrences of datetime.utcnow() remain in the codebase
- Log directory is auto-created; scripts do not fail if E:\Sentinel\logs\ does not exist
- No existing function signatures changed; scripts remain backward-compatible
</success_criteria>

<output>
After completion, create .planning/phases/01-code-hardening/01-01-SUMMARY.md
</output>
