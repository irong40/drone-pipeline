# Sentinel Aerial Inspections — Drone Pipeline Dependencies

# Core
Pillow>=11.0.0           # EXIF extraction fallback (ingest.py, platform_detect.py) — pinned >=11 for upstream CVE patches
pyexiftool>=0.5.6        # XMP-drone-dji metadata, M4E/M3E disambiguation (platform_detect.py)
requests>=2.32.0         # HTTP webhooks (ingest_sorter.py, folder_watcher.py) — pinned >=2.32 for upstream CVE patches

# File watching
watchdog>=4.0.0          # Filesystem monitoring (folder_watcher.py)

# Supabase
supabase>=2.0.0          # Database client (srt_telemetry_parser.py, video_qa.py, video_format_export.py)

# Google Drive
google-api-python-client>=2.100.0  # Drive API (gdrive_upload.py, archive_sync.py)
google-auth>=2.23.0                # Service account auth (gdrive_upload.py, archive_sync.py)

# Windows service (folder_watcher.py --install-service)
pywin32>=306              # Windows service management (folder_watcher_service.py)

# Dev / Testing
pytest>=7.4              # Test runner — 7.4 is stable LTS; pythonpath= config requires >=7.0
pytest-mock>=3.12        # mocker fixture with auto-teardown of patches (mock_ffmpeg in conftest.py)
pytest-cov>=4.1          # Coverage reporting — enables pytest --cov flags from Phase 3+
pytest-tmp-files         # Declared to satisfy TEST-02; built-in tmp_path fixture used in practice

# Note: ExifTool must be installed separately and available on PATH
# Download from https://exiftool.org/ — used by pyexiftool for XMP metadata
# Note: FFmpeg/ffprobe must be installed separately and available on PATH
# Download from https://ffmpeg.org/download.html
# Used by: video_color_grade.py, video_format_export.py, video_proxy_gen.py,
#          video_metadata.py, platform_detect.py
